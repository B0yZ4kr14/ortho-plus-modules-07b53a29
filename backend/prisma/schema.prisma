generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["auth", "public", "tenant_data"]
}

model abuse_reports {
  abuse_type   String
  auto_blocked Boolean?
  created_at   DateTime @default(now())
  details      Json?
  endpoint     String
  id           Int      @id @default(autoincrement())
  ip_address   Json
  resolved     Boolean?
  resolved_at  String?
  resolved_by  String?
  severity     String
  user_id      String?

  @@schema("public")
}

model public_admin_configurations {
  clinic_id   String
  config_data Json
  config_type String
  created_at  DateTime? @default(now())
  created_by  String?
  id          String    @id
  is_active   Boolean?
  updated_at  DateTime?

  @@map("admin_configurations")
  @@schema("public")
}

model public_analises_radiograficas {
  ai_model_version      String?
  ai_processing_time_ms Int?
  auto_approved         Boolean?
  clinic_id             String
  confidence_score      Int?
  created_at            DateTime @default(now())
  created_by            String
  feedback_comments     String?
  feedback_rating       Int?
  id                    String   @id
  imagem_storage_path   String
  imagem_url            String
  observacoes_dentista  String?
  patient_id            String
  problemas_detectados  Int?
  prontuario_id         String?
  resultado_ia          Json?
  revisado_em           String?
  revisado_por          String?
  revisado_por_dentista Boolean?
  status_analise        String
  tipo_radiografia      String
  updated_at            DateTime

  @@map("analises_radiograficas")
  @@schema("public")
}

model public_analises_radiograficas_history {
  ai_model_version     String?
  analise_id           String
  confidence_score     Int?
  created_at           DateTime @default(now())
  created_by           String
  id                   String   @id
  problemas_detectados Int?
  resultado_ia         Json
  versao               Int

  @@map("analises_radiograficas_history")
  @@schema("public")
}

model public_appointment_confirmations {
  appointment_id      String
  confirmation_method String
  confirmed_at        String?
  created_at          DateTime @default(now())
  error_message       String?
  id                  String   @id
  message_content     String?
  phone_number        String?
  sent_at             String?
  status              String

  @@map("appointment_confirmations")
  @@schema("public")
}

model public_appointment_reminders {
  appointment_id   String
  created_at       DateTime @default(now())
  error_message    String?
  id               String   @id
  message_template String
  phone_number     String?
  reminder_type    String
  scheduled_for    String
  sent_at          String?
  status           String

  @@map("appointment_reminders")
  @@schema("public")
}

model public_appointments {
  clinic_id    String
  created_at   DateTime @default(now())
  created_by   String
  dentist_id   String
  description  String?
  end_time     String
  id           String   @id
  patient_id   String
  start_time   String
  status       String
  title        String
  treatment_id String?
  updated_at   DateTime

  @@map("appointments")
  @@schema("public")
}

model public_architecture_decision_records {
  adr_number              Int
  alternatives_considered String?
  clinic_id               String
  consequences            String
  context                 String
  created_at              DateTime @default(now())
  created_by              String
  decided_at              String?
  decided_by              String?
  decision                String
  id                      String   @id
  status                  String
  superseded_by_adr_id    String?
  supersedes_adr_id       String?
  tags                    String[]
  title                   String
  updated_at              DateTime

  @@map("architecture_decision_records")
  @@schema("public")
}

model public_audit_logs {
  action           String
  action_type      String?
  affected_records Json?
  clinic_id        String?
  created_at       DateTime @default(now())
  details          Json?
  id               Int      @id @default(autoincrement())
  ip_address       Json
  target_module_id Int?
  user_agent       String?
  user_id          String?

  @@map("audit_logs")
  @@schema("public")
}

model public_audit_trail {
  action            String
  clinic_id         String?
  entity_id         String?
  entity_type       String
  id                Int     @id @default(autoincrement())
  ip_address        Json
  new_values        Json?
  old_values        Json?
  sensitivity_level String
  timestamp         String
  user_agent        String?
  user_id           String?

  @@map("audit_trail")
  @@schema("public")
}

model public_backup_history {
  backup_type            String
  checksum_md5           String?
  checksum_sha256        String?
  clinic_id              String
  completed_at           String?
  compression_ratio      Int?
  created_at             DateTime @default(now())
  created_by             String?
  error_message          String?
  file_path              String?
  file_size_bytes        Int?
  format                 String?
  id                     String   @id
  includes_postgres_dump Boolean?
  metadata               Json?
  parent_backup_id       String?
  restore_tested_at      String?
  retention_policy_id    String?
  status                 String
  transfer_speed_mbps    Int?
  verified_at            String?

  @@map("backup_history")
  @@schema("public")
}

model public_backup_replications {
  backup_id          String
  checksum_md5       String?
  completed_at       String?
  created_at         DateTime? @default(now())
  error_message      String?
  file_size_bytes    Int?
  id                 String    @id
  metadata           Json?
  region             String
  replication_status String
  source_clinic_id   String
  started_at         String?
  storage_path       String?
  storage_provider   String
  target_clinic_id   String
  updated_at         DateTime?

  @@map("backup_replications")
  @@schema("public")
}

model public_backup_retention_policies {
  auto_delete_enabled Boolean?
  backup_type         String
  clinic_id           String
  created_at          DateTime? @default(now())
  id                  String    @id
  keep_daily          Int
  keep_monthly        Int
  keep_weekly         Int
  keep_yearly         Int
  name                String
  updated_at          DateTime?

  @@map("backup_retention_policies")
  @@schema("public")
}

model public_backup_verification_log {
  backup_id         String
  details           Json?
  id                String  @id
  status            String
  verification_type String
  verified_at       String?

  @@map("backup_verification_log")
  @@schema("public")
}

model public_bi_dashboards {
  clinic_id                String
  created_at               DateTime @default(now())
  created_by               String
  description              String?
  id                       String   @id
  is_default               Boolean
  is_public                Boolean
  layout                   Json?
  name                     String
  refresh_interval_minutes Int?
  shared_with              String[]
  tags                     String[]
  updated_at               DateTime

  @@map("bi_dashboards")
  @@schema("public")
}

model public_bi_data_cache {
  cache_key  String
  clinic_id  String
  created_at DateTime @default(now())
  data       Json
  expires_at String
  id         String   @id
  widget_id  String?

  @@map("bi_data_cache")
  @@schema("public")
}

model public_bi_metrics {
  aggregation_period String
  calculation_type   String
  clinic_id          String
  created_at         DateTime @default(now())
  data_sources       String[]
  description        String?
  formula            String?
  id                 String   @id
  last_calculated_at String?
  metadata           Json?
  metric_key         String
  name               String
  trend              Int?
  updated_at         DateTime
  value              Int?

  @@map("bi_metrics")
  @@schema("public")
}

model public_bi_reports {
  clinic_id          String
  created_at         DateTime @default(now())
  created_by         String
  description        String?
  format             String
  id                 String   @id
  is_active          Boolean
  last_generated_at  String?
  name               String
  next_generation_at String?
  parameters         Json?
  recipients         String[]
  report_type        String
  schedule           Json?
  updated_at         DateTime

  @@map("bi_reports")
  @@schema("public")
}

model public_bi_widgets {
  cache_duration_minutes Int?
  chart_type             String?
  clinic_id              String
  created_at             DateTime @default(now())
  dashboard_id           String
  data_source            String
  display_config         Json?
  height                 Int
  id                     String   @id
  name                   String
  position_x             Int
  position_y             Int
  query_config           Json
  refresh_on_load        Boolean
  updated_at             DateTime
  widget_type            String
  width                  Int

  @@map("bi_widgets")
  @@schema("public")
}

model public_blocked_times {
  clinic_id      String
  created_at     DateTime? @default(now())
  created_by     String
  dentist_id     String
  end_datetime   String
  id             String    @id
  reason         String
  start_datetime String

  @@map("blocked_times")
  @@schema("public")
}

model public_budget_approvals {
  acao                  String
  alteracoes_realizadas Json?
  budget_id             String
  created_at            DateTime @default(now())
  id                    String   @id
  motivo                String?
  usuario_id            String
  valor_orcamento       Int
  versao                Int

  @@map("budget_approvals")
  @@schema("public")
}

model public_budget_items {
  budget_id           String
  created_at          DateTime @default(now())
  dente_regiao        String?
  desconto_percentual Int?
  desconto_valor      Int?
  descricao           String
  id                  String   @id
  observacoes         String?
  ordem               Int
  procedimento_id     String?
  quantidade          Int
  valor_total         Int
  valor_unitario      Int

  @@map("budget_items")
  @@schema("public")
}

model public_budget_versions {
  budget_id     String
  created_at    DateTime @default(now())
  created_by    String
  id            String   @id
  snapshot_data Json
  versao        Int

  @@map("budget_versions")
  @@schema("public")
}

model public_budgets {
  aprovado_em         String?
  aprovado_por        String?
  clinic_id           String
  contrato_id         String?
  convertido_contrato Boolean?
  created_at          DateTime @default(now())
  created_by          String
  data_expiracao      String?
  desconto_percentual Int?
  desconto_valor      Int?
  descricao           String?
  id                  String   @id
  motivo_rejeicao     String?
  numero_orcamento    String
  observacoes         String?
  patient_id          String
  rejeitado_em        String?
  rejeitado_por       String?
  status              String
  tipo_plano          String
  titulo              String
  updated_at          DateTime
  validade_dias       Int
  valor_subtotal      Int
  valor_total         Int

  @@map("budgets")
  @@schema("public")
}

model public_caixa_incidentes {
  boletim_ocorrencia  String?
  clinic_id           String
  created_at          DateTime? @default(now())
  data_incidente      String
  descricao           String?
  dia_semana          Int
  horario_incidente   String
  id                  String    @id
  metadata            Json?
  tipo_incidente      String
  valor_caixa_momento Int?
  valor_perdido       Int?

  @@map("caixa_incidentes")
  @@schema("public")
}

model public_caixa_movimentos {
  aberto_em       String?
  caixa_id        String?
  clinic_id       String
  created_at      DateTime? @default(now())
  created_by      String
  diferenca       Int?
  fechado_em      String?
  horario_risco   String?
  id              String    @id
  motivo_sangria  String?
  observacoes     String?
  risco_calculado Int?
  status          String
  sugerido_por_ia Boolean?
  tipo            String
  valor           Int
  valor_esperado  Int?
  valor_final     Int?
  valor_inicial   Int?

  @@map("caixa_movimentos")
  @@schema("public")
}

model public_campaign_metrics {
  bounce_rate       Int?
  campaign_id       String
  click_rate        Int?
  conversion_rate   Int?
  created_at        DateTime @default(now())
  id                String   @id
  metric_date       String
  open_rate         Int?
  revenue_generated Int?
  total_clicked     Int?
  total_converted   Int?
  total_delivered   Int?
  total_errors      Int?
  total_opened      Int?
  total_sent        Int?
  unsubscribe_count Int?
  updated_at        DateTime

  @@map("campaign_metrics")
  @@schema("public")
}

model public_campaign_sends {
  campaign_id       String
  clicked_at        String?
  converted_at      String?
  created_at        DateTime @default(now())
  delivered_at      String?
  error_code        String?
  error_message     String?
  id                String   @id
  message_content   String?
  metadata          Json?
  opened_at         String?
  patient_id        String
  recipient_contact String
  recipient_name    String
  retry_count       Int?
  scheduled_for     String
  sent_at           String?
  status            String

  @@map("campaign_sends")
  @@schema("public")
}

model public_campaign_templates {
  category      String?
  clinic_id     String
  content       String
  created_at    DateTime @default(now())
  created_by    String
  description   String?
  id            String   @id
  is_default    Boolean?
  name          String
  subject       String?
  template_type String
  updated_at    DateTime
  variables     Json?

  @@map("campaign_templates")
  @@schema("public")
}

model public_campaign_triggers {
  campaign_id       String
  created_at        DateTime @default(now())
  delay_days        Int?
  delay_hours       Int?
  id                String   @id
  is_active         Boolean
  trigger_condition Json
  trigger_type      String

  @@map("campaign_triggers")
  @@schema("public")
}

model public_campanha_envios {
  aberto_em         String?
  campanha_id       String
  clicado_em        String?
  convertido_em     String?
  created_at        DateTime @default(now())
  destinatario_id   String
  destinatario_tipo String
  email             String?
  enviado_em        String?
  erro_mensagem     String?
  id                String   @id
  status_envio      String
  telefone          String?

  @@map("campanha_envios")
  @@schema("public")
}

model public_campanhas_inadimplencia {
  clinic_id         String
  created_at        DateTime @default(now())
  data_envio        String?
  id                String   @id
  inadimplente_id   String?
  mensagem_enviada  String?
  resposta_recebida Boolean?
  status            String
  tipo_campanha     String
  updated_at        DateTime
  valor_recuperado  Int?

  @@map("campanhas_inadimplencia")
  @@schema("public")
}

model public_campanhas_marketing {
  clinic_id           String
  conteudo_template   String
  created_at          DateTime @default(now())
  created_by          String
  data_fim            String?
  data_inicio         String?
  id                  String   @id
  nome                String
  segmento_alvo       Json?
  status              String
  taxa_abertura       Int?
  taxa_clique         Int?
  taxa_conversao      Int?
  tipo                String
  total_aberturas     Int?
  total_cliques       Int?
  total_conversoes    Int?
  total_destinatarios Int?
  total_enviados      Int?
  updated_at          DateTime

  @@map("campanhas_marketing")
  @@schema("public")
}

model public_clinic_modules {
  clinic_id         String
  id                Int      @id @default(autoincrement())
  is_active         Boolean
  module_catalog_id Int
  subscribed_at     String
  updated_at        DateTime

  @@map("clinic_modules")
  @@schema("public")
}

model public_clinics {
  auto_cleanup_enabled  Boolean?
  backup_retention_days Int?
  created_at            DateTime @default(now())
  id                    String   @id
  name                  String
  updated_at            DateTime

  @@map("clinics")
  @@schema("public")
}

model public_cloud_storage_configs {
  clinic_id  String
  config     Json
  created_at DateTime @default(now())
  id         String   @id
  is_active  Boolean
  provider   String
  updated_at DateTime

  @@map("cloud_storage_configs")
  @@schema("public")
}

model public_collection_actions {
  action_date        String
  action_type        String
  created_at         DateTime @default(now())
  created_by         String
  id                 String   @id
  message_content    String?
  overdue_account_id String
  response_received  String?
  scheduled_for      String?
  status             String

  @@map("collection_actions")
  @@schema("public")
}

model public_collection_automation_config {
  action_type      String
  clinic_id        String
  created_at       DateTime @default(now())
  created_by       String
  days_trigger     Int
  id               String   @id
  is_active        Boolean
  message_template String
  updated_at       DateTime

  @@map("collection_automation_config")
  @@schema("public")
}

model public_contas_pagar {
  anexo_url       String?
  categoria       String
  clinic_id       String
  created_at      DateTime @default(now())
  created_by      String?
  data_emissao    String
  data_pagamento  String?
  data_vencimento String
  descricao       String
  forma_pagamento String?
  fornecedor      String
  id              String   @id
  observacoes     String?
  parcela_numero  Int?
  parcela_total   Int?
  periodicidade   String?
  recorrente      Boolean?
  status          String
  updated_at      DateTime
  valor           Int
  valor_pago      Int?

  @@map("contas_pagar")
  @@schema("public")
}

model public_contas_receber {
  clinic_id       String
  created_at      DateTime @default(now())
  created_by      String?
  data_emissao    String
  data_pagamento  String?
  data_vencimento String
  descricao       String
  forma_pagamento String?
  id              String   @id
  observacoes     String?
  parcela_numero  Int?
  parcela_total   Int?
  patient_id      String?
  status          String
  updated_at      DateTime
  valor           Int
  valor_pago      Int?

  @@map("contas_receber")
  @@schema("public")
}

model public_contrato_anexos {
  caminho_storage String
  contrato_id     String
  created_at      DateTime @default(now())
  id              String   @id
  mime_type       String
  nome_arquivo    String
  tamanho_bytes   Int
  uploaded_by     String

  @@map("contrato_anexos")
  @@schema("public")
}

model public_contrato_templates {
  ativo                 Boolean
  clinic_id             String
  conteudo_html         String
  created_at            DateTime @default(now())
  created_by            String
  id                    String   @id
  nome                  String
  tipo_tratamento       String
  updated_at            DateTime
  variaveis_disponiveis Json?

  @@map("contrato_templates")
  @@schema("public")
}

model public_contratos {
  assinado_em                String?
  assinatura_dentista_base64 String?
  assinatura_paciente_base64 String?
  cancelado_em               String?
  clinic_id                  String
  conteudo_html              String
  created_at                 DateTime @default(now())
  created_by                 String
  data_inicio                String
  data_termino               String?
  hash_blockchain            String?
  id                         String   @id
  ip_assinatura              String?
  motivo_cancelamento        String?
  numero_contrato            String
  orcamento_id               String?
  patient_id                 String
  renovacao_automatica       Boolean?
  status                     String
  template_id                String?
  titulo                     String
  updated_at                 DateTime
  valor_contrato             Int

  @@map("contratos")
  @@schema("public")
}

model public_crm_activities {
  activity_type  String
  assigned_to    String
  clinic_id      String
  completed_date String?
  created_at     DateTime @default(now())
  description    String?
  id             String   @id
  lead_id        String
  outcome        String?
  scheduled_date String?
  status         String
  title          String
  updated_at     DateTime

  @@map("crm_activities")
  @@schema("public")
}

model public_crm_conversions {
  assigned_to          String?
  clinic_id            String
  conversion_type      String
  conversion_value     Int?
  converted_at         String
  created_at           DateTime @default(now())
  id                   String   @id
  lead_id              String
  time_to_convert_days Int?
  total_interactions   Int?

  @@map("crm_conversions")
  @@schema("public")
}

model public_crm_interactions {
  attachments      Json?
  clinic_id        String
  created_at       DateTime @default(now())
  created_by       String
  description      String
  duration_minutes Int?
  id               String   @id
  interaction_type String
  lead_id          String
  next_action      String?
  next_action_date String?
  outcome          String?
  subject          String?

  @@map("crm_interactions")
  @@schema("public")
}

model public_crm_leads {
  assigned_to          String?
  clinic_id            String
  created_at           DateTime @default(now())
  created_by           String
  email                String?
  estimated_value      Int?
  id                   String   @id
  interest_description String?
  name                 String
  next_contact_date    String?
  notes                String?
  phone                String?
  source               String
  status               String
  tags                 String[]
  updated_at           DateTime

  @@map("crm_leads")
  @@schema("public")
}

model public_crm_stages {
  average_time_days Int?
  clinic_id         String
  color             String?
  conversion_rate   Int?
  created_at        DateTime @default(now())
  description       String?
  id                String   @id
  is_active         Boolean
  name              String
  order_index       Int
  updated_at        DateTime

  @@map("crm_stages")
  @@schema("public")
}

model public_crypto_candlestick_data {
  close_price Int
  close_time  String
  coin_type   String
  created_at  DateTime @default(now())
  high_price  Int
  id          String   @id
  interval    String
  low_price   Int
  open_price  Int
  open_time   String
  volume      Int

  @@map("crypto_candlestick_data")
  @@schema("public")
}

model public_crypto_payments {
  amount_brl      Int
  checkout_link   String?
  clinic_id       String
  confirmations   Int?
  confirmed_at    String?
  created_at      DateTime? @default(now())
  created_by      String?
  crypto_amount   Int?
  crypto_currency String?
  currency        String
  expires_at      String?
  id              String    @id
  invoice_id      String
  metadata        Json?
  order_id        String
  qr_code_data    String?
  status          String
  transaction_id  String?
  updated_at      DateTime?

  @@map("crypto_payments")
  @@schema("public")
}

model public_crypto_price_alerts {
  alert_type              String
  auto_convert_on_trigger Boolean?
  cascade_enabled         Boolean?
  cascade_group_id        String?
  cascade_order           Int?
  clinic_id               String
  coin_type               String
  conversion_percentage   Int?
  created_at              DateTime @default(now())
  created_by              String
  id                      String   @id
  is_active               Boolean
  last_triggered_at       String?
  notification_method     String[]
  stop_loss_enabled       Boolean?
  target_rate_brl         Int
  updated_at              DateTime

  @@map("crypto_price_alerts")
  @@schema("public")
}

model public_dentist_schedules {
  clinic_id   String
  created_at  DateTime @default(now())
  created_by  String
  day_of_week Int
  dentist_id  String
  end_time    String
  id          String   @id
  is_active   Boolean
  start_time  String
  updated_at  DateTime

  @@map("dentist_schedules")
  @@schema("public")
}

model public_estoque_pedidos {
  clinic_id              String
  created_at             DateTime @default(now())
  created_by             String
  data_pedido            String
  data_prevista_entrega  String?
  data_recebimento       String?
  fornecedor_id          String
  gerado_automaticamente Boolean?
  id                     String   @id
  numero_pedido          String
  observacoes            String?
  status                 String
  tipo                   String
  updated_at             DateTime
  valor_total            Int?

  @@map("estoque_pedidos")
  @@schema("public")
}

model public_estoque_pedidos_config {
  clinic_id              String
  created_at             DateTime @default(now())
  dias_entrega_estimados Int?
  gerar_automaticamente  Boolean?
  id                     String   @id
  ponto_pedido           Int
  produto_id             String
  quantidade_reposicao   Int
  updated_at             DateTime

  @@map("estoque_pedidos_config")
  @@schema("public")
}

model public_estoque_pedidos_itens {
  created_at          DateTime @default(now())
  id                  String   @id
  observacoes         String?
  pedido_id           String
  preco_unitario      Int
  produto_id          String
  quantidade          Int
  quantidade_recebida Int?
  valor_total         Int

  @@map("estoque_pedidos_itens")
  @@schema("public")
}

model public_fechamento_caixa {
  arquivo_sped_gerado_em String?
  arquivo_sped_path      String?
  caixa_movimento_id     String
  clinic_id              String
  created_at             DateTime? @default(now())
  created_by             String
  data_fechamento        String
  divergencia            Int
  id                     String    @id
  observacoes            String?
  percentual_divergencia Int?
  quantidade_nfce        Int
  quantidade_vendas_pdv  Int
  total_nfce_emitidas    Int
  total_sangrias         Int
  total_suprimentos      Int
  total_vendas_pdv       Int
  vendas_sem_nfce        Int

  @@map("fechamento_caixa")
  @@schema("public")
}

model public_financial_categories {
  active     Boolean?
  clinic_id  String
  color      String?
  created_at DateTime @default(now())
  icon       String?
  id         String   @id
  name       String
  type       String
  updated_at DateTime

  @@map("financial_categories")
  @@schema("public")
}

model public_financial_transactions {
  amount           Int
  category         String
  clinic_id        String
  created_at       DateTime @default(now())
  created_by       String?
  description      String
  id               String   @id
  notes            String?
  payment_method   String?
  status           String
  tags             String[]
  transaction_date String
  type             String
  updated_at       DateTime

  @@map("financial_transactions")
  @@schema("public")
}

model public_fiscal_config {
  ambiente                 String
  certificado_digital      String?
  clinic_id                String
  cnpj                     String
  codigo_regime_tributario Int?
  contingencia_enabled     Boolean?
  contingencia_motivo      String?
  created_at               DateTime? @default(now())
  csc_id                   String?
  csc_token                String?
  email_contabilidade      String?
  endereco                 Json?
  id                       String    @id
  inscricao_estadual       String?
  is_active                Boolean?
  nome_fantasia            String?
  numero_ultimo_nfce       Int?
  razao_social             String
  regime_tributario        String
  senha_certificado        String?
  serie_nfce               Int?
  tipo_emissao             String
  updated_at               DateTime?

  @@map("fiscal_config")
  @@schema("public")
}

model public_funcionarios {
  avatar_url       String?
  cargo            String
  celular          String
  clinic_id        String
  cpf              String
  created_at       DateTime @default(now())
  data_admissao    String
  data_nascimento  String
  dias_trabalho    Int[]
  email            String
  endereco         Json
  horario_trabalho Json
  id               String   @id
  nome             String
  observacoes      String?
  permissoes       Json
  rg               String?
  salario          Int
  sexo             String
  status           String
  telefone         String
  updated_at       DateTime
  user_id          String?

  @@map("funcionarios")
  @@schema("public")
}

model public_github_events {
  clinic_id    String
  created_at   DateTime @default(now())
  event_data   Json
  event_type   String
  id           String   @id
  triggered_by String?

  @@map("github_events")
  @@schema("public")
}

model public_historico_clinico {
  created_at         DateTime @default(now())
  created_by         String
  dados_estruturados Json?
  descricao          String
  id                 String   @id
  prontuario_id      String
  tipo               String
  titulo             String
  updated_at         DateTime

  @@map("historico_clinico")
  @@schema("public")
}

model public_inadimplentes {
  clinic_id          String
  created_at         DateTime @default(now())
  dias_atraso        Int
  email              String?
  id                 String   @id
  nome_paciente      String
  observacoes        String?
  patient_id         String
  status             String
  telefone           String?
  ultima_cobranca    String?
  updated_at         DateTime
  valor_total_devido Int

  @@map("inadimplentes")
  @@schema("public")
}

model public_integracao_contabil_config {
  api_key             String?
  api_secret          String?
  api_url             String
  ativo               Boolean
  clinic_id           String
  codigo_empresa      String?
  created_at          DateTime @default(now())
  email_contador      String?
  enviar_nfce_dados   Boolean
  enviar_sped_fiscal  Boolean
  envio_automatico    Boolean
  id                  String   @id
  metadata            Json?
  periodicidade_envio String
  software            String
  updated_at          DateTime

  @@map("integracao_contabil_config")
  @@schema("public")
}

model public_integracao_contabil_envios {
  arquivo_path       String?
  arquivo_size_bytes Int?
  clinic_id          String
  config_id          String
  created_at         DateTime @default(now())
  enviado_em         String?
  erro_mensagem      String?
  id                 String   @id
  max_tentativas     Int
  periodo_referencia String
  response_data      Json?
  status             String
  tentativas         Int
  tipo_documento     String
  updated_at         DateTime

  @@map("integracao_contabil_envios")
  @@schema("public")
}

model public_inventario_agendamentos {
  ativo                 Boolean
  categorias_ids        String[]
  clinic_id             String
  created_at            DateTime @default(now())
  created_by            String
  dia_execucao          Int?
  dia_semana            Int?
  id                    String   @id
  nome                  String
  notificar_dias_antes  Int?
  notificar_responsavel Boolean?
  observacoes           String?
  periodicidade         String
  proxima_execucao      String?
  responsavel           String
  tipo_inventario       String
  ultima_execucao       String?
  updated_at            DateTime

  @@map("inventario_agendamentos")
  @@schema("public")
}

model public_inventario_itens {
  contado_em             String?
  contado_por            String?
  created_at             DateTime @default(now())
  divergencia            Int?
  id                     String   @id
  inventario_id          String
  lote                   String?
  observacoes            String?
  percentual_divergencia Int?
  produto_id             String
  produto_nome           String
  quantidade_fisica      Int?
  quantidade_sistema     Int
  valor_divergencia      Int?
  valor_unitario         Int

  @@map("inventario_itens")
  @@schema("public")
}

model public_inventarios {
  clinic_id                String
  created_at               DateTime @default(now())
  created_by               String
  data                     String
  divergencias_encontradas Int?
  id                       String   @id
  itens_contados           Int?
  numero                   String
  observacoes              String?
  responsavel              String
  status                   String
  tipo                     String
  total_itens              Int?
  updated_at               DateTime
  valor_divergencias       Int?

  @@map("inventarios")
  @@schema("public")
}

model public_lead_interacoes {
  agendou_avaliacao Boolean?
  created_at        DateTime @default(now())
  created_by        String
  descricao         String
  id                String   @id
  lead_id           String
  proximo_passo     String?
  resultado         String?
  tipo              String

  @@map("lead_interacoes")
  @@schema("public")
}

model public_lead_tags {
  created_at DateTime @default(now())
  id         String   @id
  lead_id    String
  tag        String

  @@map("lead_tags")
  @@schema("public")
}

model public_leads {
  atribuido_a      String?
  clinic_id        String
  created_at       DateTime @default(now())
  created_by       String?
  email            String?
  id               String   @id
  interesse        String?
  motivo_perda     String?
  nome             String
  observacoes      String?
  origem           String
  proximo_followup String?
  score_qualidade  Int?
  status_funil     String
  telefone         String?
  temperatura      String
  ultimo_contato   String?
  updated_at       DateTime
  utm_campaign     String?
  utm_medium       String?
  utm_source       String?
  valor_estimado   Int?
  whatsapp         String?

  @@map("leads")
  @@schema("public")
}

model public_lgpd_consents {
  accepted     Boolean
  accepted_at  String?
  accepted_by  String?
  clinic_id    String
  consent_text String
  consent_type String
  created_at   DateTime @default(now())
  expires_at   String?
  id           String   @id
  ip_address   String?
  metadata     Json?
  patient_id   String
  revoked      Boolean?
  revoked_at   String?
  revoked_by   String?
  updated_at   DateTime
  user_agent   String?
  version      Int

  @@map("lgpd_consents")
  @@schema("public")
}

model public_lgpd_data_consents {
  clinic_id    String
  consent_type String
  created_at   DateTime @default(now())
  expires_at   String?
  granted      Boolean
  granted_at   String?
  id           String   @id
  patient_id   String
  revoked_at   String?

  @@map("lgpd_data_consents")
  @@schema("public")
}

model public_lgpd_data_exports {
  clinic_id       String
  created_at      DateTime @default(now())
  download_count  Int?
  downloaded_at   String?
  error_message   String?
  expires_at      String?
  export_type     String
  file_format     String?
  file_path       String?
  file_size_bytes Int?
  generated_at    String?
  generated_by    String
  id              String   @id
  metadata        Json?
  patient_id      String
  request_id      String?
  status          String
  tables_included String[]
  updated_at      DateTime

  @@map("lgpd_data_exports")
  @@schema("public")
}

model public_lgpd_data_requests {
  clinic_id        String
  completed_at     String?
  created_at       DateTime @default(now())
  data_export_id   String?
  description      String?
  id               String   @id
  metadata         Json?
  patient_id       String
  rejection_reason String?
  request_type     String
  requested_at     String
  requested_by     String
  responded_at     String?
  responded_by     String?
  response         String?
  status           String
  updated_at       DateTime

  @@map("lgpd_data_requests")
  @@schema("public")
}

model public_login_attempts {
  attempted_at             String
  created_at               DateTime @default(now())
  device_fingerprint       String?
  email                    String
  id                       Int      @id @default(autoincrement())
  ip_address               String?
  ip_geolocation           Json?
  session_duration_seconds Int?
  success                  Boolean
  user_agent               String?

  @@map("login_attempts")
  @@schema("public")
}

model public_marketing_campaigns {
  campaign_type    String
  channel          String
  clinic_id        String
  created_at       DateTime @default(now())
  created_by       String
  description      String?
  end_date         String?
  id               String   @id
  last_sent_at     String?
  metadata         Json?
  name             String
  schedule_config  Json?
  send_immediately Boolean?
  start_date       String?
  status           String
  target_audience  Json?
  template_id      String?
  trigger_config   Json?
  updated_at       DateTime

  @@map("marketing_campaigns")
  @@schema("public")
}

model public_module_catalog {
  category    String
  created_at  DateTime @default(now())
  description String?
  icon        String?
  id          Int      @id @default(autoincrement())
  module_key  String
  name        String

  @@map("module_catalog")
  @@schema("public")
}

model public_module_configuration_templates {
  created_at  DateTime @default(now())
  description String?
  icon        String?
  id          String   @id
  is_active   Boolean
  modules     Json
  name        String
  specialty   String
  updated_at  DateTime

  @@map("module_configuration_templates")
  @@schema("public")
}

model public_module_dependencies {
  created_at           DateTime @default(now())
  depends_on_module_id Int
  id                   Int      @id @default(autoincrement())
  module_id            Int

  @@map("module_dependencies")
  @@schema("public")
}

model public_movimentacoes_estoque {
  clinic_id           String
  created_at          DateTime @default(now())
  id                  String   @id
  motivo              String?
  observacoes         String?
  produto_id          String
  quantidade          Int
  quantidade_anterior Int
  quantidade_atual    Int
  tipo                String
  usuario_id          String
  valor_total         Int
  valor_unitario      Int

  @@map("movimentacoes_estoque")
  @@schema("public")
}

model public_nfce_carta_correcao {
  clinic_id     String
  codigo_status String?
  correcao      String
  created_at    DateTime? @default(now())
  created_by    String
  data_evento   String?
  id            String    @id
  motivo        String?
  nfce_id       String
  protocolo     String?
  sequencia     Int
  status        String
  xml_evento    String?

  @@map("nfce_carta_correcao")
  @@schema("public")
}

model public_nfce_contingencia {
  chave_acesso             String
  clinic_id                String
  created_at               DateTime @default(now())
  emitido_em               String
  erro_sincronizacao       String?
  forma_pagamento          String
  id                       String   @id
  itens                    Json
  modo_contingencia        String
  motivo_contingencia      String
  numero_nfce              Int
  protocolo_autorizacao    String?
  serie                    Int
  sincronizado_em          String?
  status_sincronizacao     String
  tentativas_sincronizacao Int
  updated_at               DateTime
  valor_total              Int
  venda_id                 String?
  xml_nfce                 String

  @@map("nfce_contingencia")
  @@schema("public")
}

model public_nfce_emitidas {
  ambiente              String
  chave_acesso          String
  clinic_id             String
  contingencia          Boolean?
  created_at            DateTime? @default(now())
  data_cancelamento     String?
  data_emissao          String?
  error_message         String?
  id                    String    @id
  metadata              Json?
  motivo_cancelamento   String?
  numero_nfce           Int
  pdf_url               String?
  protocolo_autorizacao String?
  qrcode_url            String?
  serie                 Int
  status                String
  tipo_emissao          String
  updated_at            DateTime?
  valor_total           Int
  venda_id              String
  xml_cancelamento      String?
  xml_nfce              String?

  @@map("nfce_emitidas")
  @@schema("public")
}

model public_nfce_inutilizacao {
  ano               Int
  clinic_id         String
  codigo_status     String?
  created_at        DateTime? @default(now())
  created_by        String
  data_inutilizacao String?
  id                String    @id
  justificativa     String
  motivo            String?
  numero_final      Int
  numero_inicial    Int
  protocolo         String?
  serie             Int
  status            String
  xml_inutilizacao  String?

  @@map("nfce_inutilizacao")
  @@schema("public")
}

model public_notas_fiscais {
  chave_acesso      String?
  clinic_id         String
  created_at        DateTime @default(now())
  created_by        String?
  data_emissao      String
  data_recebimento  String?
  destinatario_cnpj String?
  destinatario_nome String?
  emitente_cnpj     String
  emitente_nome     String
  id                String   @id
  numero            String
  observacoes       String?
  pdf_url           String?
  serie             String?
  status            String
  tipo              String
  updated_at        DateTime
  valor_icms        Int?
  valor_iss         Int?
  valor_total       Int
  xml_url           String?

  @@map("notas_fiscais")
  @@schema("public")
}

model public_notifications {
  clinic_id  String
  created_at DateTime @default(now())
  id         String   @id
  lida       Boolean
  lida_em    String?
  link_acao  String?
  mensagem   String
  tipo       String
  titulo     String
  user_id    String?

  @@map("notifications")
  @@schema("public")
}

model public_odontogramas {
  clinic_id     String
  created_at    DateTime @default(now())
  history       Json
  id            String   @id
  last_updated  String
  prontuario_id String
  teeth         Json
  updated_at    DateTime

  @@map("odontogramas")
  @@schema("public")
}

model public_onboarding_analytics {
  clinic_id          String
  created_at         DateTime @default(now())
  event_type         String
  id                 String   @id
  metadata           Json?
  step_name          String?
  step_number        Int?
  time_spent_seconds Int?
  user_id            String

  @@map("onboarding_analytics")
  @@schema("public")
}

model public_orcamento_itens {
  created_at      DateTime @default(now())
  dente_codigo    String?
  descricao       String
  id              String   @id
  observacoes     String?
  orcamento_id    String
  ordem           Int
  procedimento_id String?
  quantidade      Int
  valor_total     Int
  valor_unitario  Int

  @@map("orcamento_itens")
  @@schema("public")
}

model public_orcamento_pagamento {
  created_at      DateTime @default(now())
  forma_pagamento String[]
  id              String   @id
  numero_parcelas Int?
  observacoes     String?
  orcamento_id    String
  tipo_pagamento  String
  valor_entrada   Int?
  valor_parcela   Int?

  @@map("orcamento_pagamento")
  @@schema("public")
}

model public_orcamento_visualizacoes {
  duracao_segundos Int?
  id               String  @id
  ip_address       String?
  orcamento_id     String
  user_agent       String?
  visualizado_em   String

  @@map("orcamento_visualizacoes")
  @@schema("public")
}

model public_orcamentos {
  aprovado_em         String?
  aprovado_por        String?
  clinic_id           String
  convertido_em       String?
  created_at          DateTime @default(now())
  created_by          String
  data_validade       String
  desconto_percentual Int?
  desconto_valor      Int?
  descricao           String?
  id                  String   @id
  motivo_rejeicao     String?
  numero_orcamento    String
  observacoes         String?
  patient_id          String
  prontuario_id       String?
  rejeitado_em        String?
  status              String
  tipo_plano          String
  titulo              String
  updated_at          DateTime
  validade_dias       Int
  valor_final         Int
  valor_total         Int

  @@map("orcamentos")
  @@schema("public")
}

model public_overdue_accounts {
  clinic_id        String
  conta_receber_id String
  created_at       DateTime @default(now())
  days_overdue     Int
  id               String   @id
  interest_amount  Int?
  original_amount  Int
  patient_id       String
  penalty_amount   Int?
  remaining_amount Int
  risk_level       String
  status           String
  total_amount     Int
  updated_at       DateTime

  @@map("overdue_accounts")
  @@schema("public")
}

model public_patient_accounts {
  ativo             Boolean
  created_at        DateTime @default(now())
  email             String
  email_verificado  Boolean
  id                String   @id
  patient_id        String
  senha_hash        String
  token_verificacao String?
  ultimo_acesso     String?
  updated_at        DateTime

  @@map("patient_accounts")
  @@schema("public")
}

model public_patient_messages {
  anexos         Json?
  clinic_id      String
  created_at     DateTime @default(now())
  id             String   @id
  lida           Boolean
  lida_em        String?
  mensagem       String
  patient_id     String
  remetente_id   String
  remetente_tipo String

  @@map("patient_messages")
  @@schema("public")
}

model public_patient_notifications {
  created_at DateTime @default(now())
  id         String   @id
  lida       Boolean
  lida_em    String?
  link_acao  String?
  mensagem   String
  patient_id String
  tipo       String
  titulo     String

  @@map("patient_notifications")
  @@schema("public")
}

model public_patient_preferences {
  id                      String   @id
  idioma                  String?
  lembrete_consulta_horas Int?
  notificacoes_email      Boolean?
  notificacoes_push       Boolean?
  notificacoes_sms        Boolean?
  notificacoes_whatsapp   Boolean?
  patient_id              String
  tema                    String?
  updated_at              DateTime

  @@map("patient_preferences")
  @@schema("public")
}

model public_patient_sessions {
  created_at DateTime @default(now())
  expires_at String
  id         String   @id
  patient_id String
  token      String

  @@map("patient_sessions")
  @@schema("public")
}

model public_patient_status_history {
  changed_at  String?
  changed_by  String?
  created_at  DateTime? @default(now())
  from_status String?
  id          String    @id
  notes       String?
  patient_id  String
  reason      String?
  to_status   String

  @@map("patient_status_history")
  @@schema("public")
}

model public_patients {
  address_city                   String?
  address_complement             String?
  address_country                String?
  address_neighborhood           String?
  address_number                 String?
  address_state                  String?
  address_street                 String?
  address_zipcode                String?
  alcohol_frequency              String?
  allergies_list                 String[]
  birth_date                     String
  bleeding_disorder_details      String?
  blood_pressure_diastolic       Int?
  blood_pressure_systolic        Int?
  blood_type                     String?
  bmi                            Int?
  campanha_origem_id             String?
  campanha_origem_nome           String?
  canal_captacao                 String?
  cardiovascular_details         String?
  cargo                          String?
  churn_risk_score               Int?
  clinic_id                      String
  clinical_observations          String?
  cnpj_empresa                   String?
  cpf                            String?
  created_at                     DateTime @default(now())
  created_by                     String?
  current_medications            String[]
  data_conversao                 String?
  data_primeiro_contato          String?
  data_qualificacao              String?
  data_sharing_consent           Boolean?
  diabetes_controlled            Boolean?
  diabetes_type                  String?
  education_level                String?
  email                          String?
  emergency_contact_name         String?
  emergency_contact_relationship String?
  empresa                        String?
  evento_captacao                String?
  first_appointment_date         String?
  full_name                      String
  gender                         String?
  gum_condition                  String?
  has_alcohol_habit              Boolean?
  has_allergies                  Boolean?
  has_bleeding_disorder          Boolean?
  has_cardiovascular_disease     Boolean?
  has_diabetes                   Boolean?
  has_hepatitis                  Boolean?
  has_hiv                        Boolean?
  has_hypertension               Boolean?
  has_medication_allergy         Boolean?
  has_smoking_habit              Boolean?
  has_systemic_disease           Boolean?
  heart_rate                     Int?
  height_cm                      Int?
  hepatitis_type                 String?
  hypertension_controlled        Boolean?
  id                             String   @id
  image_usage_consent            Boolean?
  indicado_por                   String?
  indicado_por_dentista_id       String?
  indicado_por_paciente_id       String?
  is_breastfeeding               Boolean?
  is_pregnant                    Boolean?
  last_appointment_date          String?
  lgpd_consent                   Boolean?
  lgpd_consent_date              String?
  main_complaint                 String?
  marital_status                 String?
  marketing_campaign             String?
  marketing_event                String?
  marketing_promoter             String?
  marketing_source               String?
  marketing_telemarketing_agent  String?
  medication_allergies           String[]
  nationality                    String?
  occupation                     String?
  oral_hygiene_quality           String?
  origem_lead                    String?
  pain_level                     Int?
  patient_code                   String?
  payment_status                 String?
  phone_emergency                String?
  phone_primary                  String
  phone_secondary                String?
  preferred_payment_method       String?
  pregnancy_trimester            Int?
  promotor_id                    String?
  promotor_nome                  String?
  propensao_indicacao            Int?
  referral_source                String?
  rg                             String?
  risk_level                     String?
  risk_score_anesthetic          Int?
  risk_score_medical             Int?
  risk_score_overall             Int?
  risk_score_surgical            Int?
  smoking_frequency              String?
  social_name                    String?
  status                         String?
  systemic_diseases              String[]
  total_appointments             Int?
  total_debt                     Int?
  total_paid                     Int?
  treatment_consent              Boolean?
  updated_at                     DateTime
  updated_by                     String?
  valor_lifetime                 Int?
  valor_ticket_medio             Int?
  weight_kg                      Int?

  @@map("patients")
  @@schema("public")
}

model public_payment_methods {
  active          Boolean?
  clinic_id       String
  config          Json?
  created_at      DateTime @default(now())
  id              String   @id
  name            String
  taxa_fixa       Int?
  taxa_percentual Int?
  type            String
  updated_at      DateTime

  @@map("payment_methods")
  @@schema("public")
}

model public_payment_negotiations {
  accepted_at         String?
  clinic_id           String
  completed_at        String?
  created_at          DateTime @default(now())
  created_by          String
  discount_amount     Int?
  discount_percentage Int?
  first_payment_date  String?
  id                  String   @id
  installments        Int?
  negotiated_amount   Int
  original_amount     Int
  overdue_account_id  String
  patient_id          String
  status              String
  updated_at          DateTime

  @@map("payment_negotiations")
  @@schema("public")
}

model public_pdv_produtos {
  cest              String?
  cfop              String?
  clinic_id         String
  codigo            String
  cofins_aliquota   Int?
  controla_estoque  Boolean?
  created_at        DateTime? @default(now())
  cst_cofins        String?
  cst_icms          String?
  cst_pis           String?
  descricao         String
  estoque_atual     Int?
  estoque_minimo    Int?
  icms_aliquota     Int?
  id                String    @id
  is_active         Boolean?
  ncm               String?
  origem_mercadoria Int?
  pis_aliquota      Int?
  tipo              String
  unidade_medida    String?
  updated_at        DateTime?
  valor_unitario    Int

  @@map("pdv_produtos")
  @@schema("public")
}

model public_pdv_vendas {
  caixa_movimento_id String?
  clinic_id          String
  created_at         DateTime @default(now())
  forma_pagamento    String
  id                 String   @id
  metadata           Json?
  numero_venda       String
  observacoes        String?
  status             String
  updated_at         DateTime
  valor_total        Int

  @@map("pdv_vendas")
  @@schema("public")
}

model public_pep_anexos {
  caminho_storage String
  created_at      DateTime @default(now())
  descricao       String?
  historico_id    String?
  id              String   @id
  mime_type       String
  nome_arquivo    String
  prontuario_id   String
  tamanho_bytes   Int
  tipo_arquivo    String
  uploaded_by     String

  @@map("pep_anexos")
  @@schema("public")
}

model public_pep_assinaturas {
  assinatura_base64 String
  historico_id      String?
  id                String  @id
  ip_address        String?
  prontuario_id     String
  signed_at         String
  signed_by         String
  tipo_documento    String
  user_agent        String?

  @@map("pep_assinaturas")
  @@schema("public")
}

model public_pep_evolucoes {
  created_at    DateTime @default(now())
  created_by    String
  data_evolucao String
  descricao     String
  id            String   @id
  tipo          String
  tratamento_id String

  @@map("pep_evolucoes")
  @@schema("public")
}

model public_pep_odontograma {
  created_at     DateTime @default(now())
  created_by     String
  dente_codigo   String
  faces_afetadas String[]
  id             String   @id
  observacoes    String?
  prontuario_id  String
  status         String
  updated_at     DateTime

  @@map("pep_odontograma")
  @@schema("public")
}

model public_pep_odontograma_data {
  created_at    DateTime @default(now())
  created_by    String
  id            String   @id
  notes         String?
  prontuario_id String
  status        String
  tooth_number  Int
  updated_at    DateTime
  updated_by    String?

  @@map("pep_odontograma_data")
  @@schema("public")
}

model public_pep_odontograma_history {
  changed_teeth Int[]
  created_at    DateTime @default(now())
  created_by    String
  description   String?
  id            String   @id
  prontuario_id String
  snapshot_data Json

  @@map("pep_odontograma_history")
  @@schema("public")
}

model public_pep_tooth_surfaces {
  created_at          DateTime @default(now())
  id                  String   @id
  odontograma_data_id String
  status              String
  surface             String
  updated_at          DateTime

  @@map("pep_tooth_surfaces")
  @@schema("public")
}

model public_pep_tratamentos {
  created_at      DateTime @default(now())
  created_by      String
  data_conclusao  String?
  data_inicio     String
  dente_codigo    String?
  descricao       String
  id              String   @id
  observacoes     String?
  procedimento_id String?
  prontuario_id   String
  status          String
  titulo          String
  updated_at      DateTime
  valor_estimado  Int?

  @@map("pep_tratamentos")
  @@schema("public")
}

model public_permission_audit_logs {
  action            String
  clinic_id         String
  created_at        DateTime @default(now())
  details           Json?
  id                String   @id
  module_catalog_id Int?
  target_user_id    String
  template_name     String?
  user_id           String

  @@map("permission_audit_logs")
  @@schema("public")
}

model public_permission_templates {
  created_at  DateTime @default(now())
  description String?
  icon        String?
  id          String   @id
  module_keys String[]
  name        String

  @@map("permission_templates")
  @@schema("public")
}

model public_prescricoes_remotas {
  assinatura_digital     String?
  created_at             DateTime @default(now())
  descricao              String
  enviado_em             String?
  enviado_para_paciente  Boolean?
  id                     String   @id
  instrucoes             String?
  medicamento_dosagem    String?
  medicamento_duracao    String?
  medicamento_frequencia String?
  medicamento_nome       String?
  teleconsulta_id        String
  tipo                   String

  @@map("prescricoes_remotas")
  @@schema("public")
}

model public_problemas_radiograficos {
  analise_id          String
  confianca           Int?
  created_at          DateTime @default(now())
  dente_codigo        String?
  descricao           String?
  id                  String   @id
  localizacao         String?
  severidade          String
  sugestao_tratamento String?
  tipo_problema       String
  urgente             Boolean?

  @@map("problemas_radiograficos")
  @@schema("public")
}

model public_procedimento_templates {
  categoria              String
  clinic_id              String?
  created_at             DateTime @default(now())
  created_by             String
  descricao              String?
  id                     String   @id
  is_public              Boolean?
  nome                   String
  steps                  Json
  tags                   String[]
  tempo_estimado_minutos Int
  updated_at             DateTime
  valor_sugerido         Int

  @@map("procedimento_templates")
  @@schema("public")
}

model public_produtos {
  ativo             Boolean
  categoria         String
  clinic_id         String
  codigo_barras     String?
  created_at        DateTime @default(now())
  descricao         String?
  fornecedor        String?
  id                String   @id
  localizacao       String?
  nome              String
  observacoes       String?
  quantidade_atual  Int
  quantidade_minima Int
  unidade_medida    String
  updated_at        DateTime
  valor_unitario    Int

  @@map("produtos")
  @@schema("public")
}

model public_profiles {
  app_role   String
  avatar_url String?
  clinic_id  String?
  created_at DateTime @default(now())
  full_name  String?
  id         String   @id
  is_active  Boolean
  phone      String?
  updated_at DateTime

  @@map("profiles")
  @@schema("public")
}

model public_prontuarios {
  clinic_id    String
  created_at   DateTime @default(now())
  created_by   String
  id           String   @id
  patient_id   String
  patient_name String
  updated_at   DateTime

  @@map("prontuarios")
  @@schema("public")
}

model public_radiografia_ai_feedback {
  analise_id          String
  anotacoes_dentista  Json?
  clinic_id           String
  created_at          DateTime @default(now())
  created_by          String
  diagnostico_correto String?
  falsos_negativos    Json?
  falsos_positivos    Json?
  ia_estava_correta   Boolean
  id                  String   @id
  imagem_marcada_url  String?
  usado_para_treino   Boolean?

  @@map("radiografia_ai_feedback")
  @@schema("public")
}

model public_radiografia_laudo_templates {
  clinic_id             String
  created_at            DateTime @default(now())
  created_by            String
  id                    String   @id
  is_default            Boolean?
  nome_template         String
  template_markdown     String
  tipo_radiografia      String
  variaveis_disponiveis Json?

  @@map("radiografia_laudo_templates")
  @@schema("public")
}

model public_rate_limit_config {
  created_at            DateTime @default(now())
  enabled               Boolean
  endpoint              String
  id                    Int      @id @default(autoincrement())
  max_requests_per_ip   Int
  max_requests_per_user Int
  updated_at            DateTime
  window_minutes        Int

  @@map("rate_limit_config")
  @@schema("public")
}

model public_rate_limit_log {
  created_at    DateTime @default(now())
  endpoint      String
  id            Int      @id @default(autoincrement())
  ip_address    Json
  request_count Int?
  updated_at    DateTime
  user_id       String?
  window_start  String

  @@map("rate_limit_log")
  @@schema("public")
}

model public_recalls {
  clinic_id              String
  created_at             DateTime @default(now())
  created_by             String?
  data_prevista          String
  id                     String   @id
  mensagem_personalizada String?
  metodo_notificacao     String?
  notificacao_enviada    Boolean?
  patient_id             String
  status                 String
  tipo_recall            String
  updated_at             DateTime

  @@map("recalls")
  @@schema("public")
}

model public_room_availability {
  capacity    Int
  clinic_id   String
  created_at  DateTime @default(now())
  created_by  String
  equipment   Json?
  id          String   @id
  is_active   Boolean
  room_name   String
  room_number String?
  updated_at  DateTime

  @@map("room_availability")
  @@schema("public")
}

model public_root_actions_log {
  action           String
  details          Json?
  executed_at      String
  id               Int     @id @default(autoincrement())
  ip_address       Json
  root_user_id     String
  target_record_id String?
  target_table     String?
  user_agent       String?

  @@map("root_actions_log")
  @@schema("public")
}

model public_rum_metrics {
  clinic_id    String
  created_at   DateTime @default(now())
  id           Int      @id @default(autoincrement())
  metric_name  String
  metric_value Int
  page_url     String
  rating       String
  user_agent   String?
  user_id      String?

  @@map("rum_metrics")
  @@schema("public")
}

model public_sat_mfe_config {
  ativo            Boolean
  clinic_id        String
  codigo_ativacao  String?
  created_at       DateTime @default(now())
  fabricante       String?
  id               String   @id
  ip_address       String?
  metadata         Json?
  modelo           String?
  numero_serie     String
  porta            Int?
  tipo_equipamento String
  updated_at       DateTime
  versao_software  String?

  @@map("sat_mfe_config")
  @@schema("public")
}

model public_sat_mfe_impressoes {
  chave_consulta     String?
  clinic_id          String
  codigo_autorizacao String?
  config_id          String
  created_at         DateTime @default(now())
  id                 String   @id
  mensagem_retorno   String?
  metadata           Json?
  nfce_id            String?
  numero_sessao      String?
  status             String
  tentativas         Int
  tipo_equipamento   String
  updated_at         DateTime
  venda_id           String?
  xml_enviado        String?
  xml_retorno        String?

  @@map("sat_mfe_impressoes")
  @@schema("public")
}

model public_scheduled_backups {
  backup_type            String
  clinic_id              String
  compression_enabled    Boolean
  created_at             DateTime @default(now())
  created_by             String
  day_of_month           Int?
  day_of_week            Int?
  encryption_enabled     Boolean
  frequency              String
  id                     String   @id
  include_data           Json
  includes_postgres_dump Boolean?
  is_active              Boolean
  last_run_at            String?
  local_path             String?
  max_parallel_jobs      Int?
  name                   String
  next_run_at            String
  notification_emails    String[]
  retention_policy_id    String?
  storage_config         Json?
  storage_destination    String?
  time_of_day            String
  updated_at             DateTime

  @@map("scheduled_backups")
  @@schema("public")
}

model public_scheduled_exports {
  clinic_id      String
  created_at     DateTime @default(now())
  dashboard_name String
  email          String
  export_format  String
  frequency      String
  id             String   @id
  is_active      Boolean
  last_sent_at   String?
  next_send_at   String?
  updated_at     DateTime
  user_id        String

  @@map("scheduled_exports")
  @@schema("public")
}

model public_security_audit_log {
  description       String
  id                Int     @id @default(autoincrement())
  issue_type        String
  migration_version String
  resolution        String
  resolved_at       String
  severity          String?

  @@map("security_audit_log")
  @@schema("public")
}

model public_split_payment_config {
  clinic_id       String
  created_at      DateTime @default(now())
  id              String   @id
  is_active       Boolean
  percentage      Int
  procedure_type  String?
  professional_id String
  updated_at      DateTime

  @@map("split_payment_config")
  @@schema("public")
}

model public_split_payment_details {
  created_at           DateTime @default(now())
  id                   String   @id
  net_amount           Int
  paid_at              String?
  recipient_id         String
  recipient_name       String
  recipient_type       String
  split_amount         Int
  split_transaction_id String
  status               String
  tax_withheld         Int?

  @@map("split_payment_details")
  @@schema("public")
}

model public_split_payment_recipients {
  clinic_id          String
  created_at         DateTime @default(now())
  id                 String   @id
  recipient_id       String?
  recipient_name     String
  recipient_type     String
  rule_id            String
  split_fixed_amount Int?
  split_percentage   Int?
  tax_rate           Int?
  tax_regime         String?

  @@map("split_payment_recipients")
  @@schema("public")
}

model public_split_payment_rules {
  clinic_id  String
  conditions Json
  created_at DateTime @default(now())
  created_by String
  id         String   @id
  priority   Int
  rule_name  String
  rule_type  String
  status     String
  updated_at DateTime

  @@map("split_payment_rules")
  @@schema("public")
}

model public_split_payment_transactions {
  clinic_id           String
  conta_receber_id    String?
  created_at          DateTime @default(now())
  error_message       String?
  id                  String   @id
  processed_at        String?
  rule_id             String?
  split_calculated_at String
  status              String
  total_amount        Int
  transaction_id      String?

  @@map("split_payment_transactions")
  @@schema("public")
}

model public_split_transactions {
  clinic_amount       Int
  clinic_id           String
  created_at          DateTime @default(now())
  id                  String   @id
  percentage          Int
  processed_at        String?
  professional_amount Int
  professional_id     String
  status              String
  total_amount        Int
  transaction_id      String

  @@map("split_transactions")
  @@schema("public")
}

model public_system_health_metrics {
  clinic_id   String
  id          String  @id
  metadata    Json?
  metric_type String
  recorded_at String
  unit        String?
  value       Int

  @@map("system_health_metrics")
  @@schema("public")
}

model public_teleconsultas {
  appointment_id  String?
  clinic_id       String
  conduta         String?
  created_at      DateTime @default(now())
  created_by      String
  data_agendada   String
  data_finalizada String?
  data_iniciada   String?
  dentist_id      String
  diagnostico     String?
  duracao_minutos Int?
  id              String   @id
  link_sala       String?
  motivo          String
  observacoes     String?
  patient_id      String
  recording_url   String?
  status          String
  tipo            String
  titulo          String
  updated_at      DateTime

  @@map("teleconsultas")
  @@schema("public")
}

model public_teleodonto_chat {
  attachment_type String?
  attachment_url  String?
  id              String  @id
  message_text    String
  message_type    String
  read_at         String?
  sender_id       String
  sender_role     String
  sent_at         String
  session_id      String

  @@map("teleodonto_chat")
  @@schema("public")
}

model public_teleodonto_files {
  compartilhado_com_paciente Boolean?
  descricao                  String?
  file_name                  String
  file_size_bytes            Int
  file_type                  String
  file_url                   String?
  id                         String   @id
  session_id                 String
  storage_path               String
  tipo_arquivo               String
  uploaded_at                String
  uploaded_by                String

  @@map("teleodonto_files")
  @@schema("public")
}

model public_teleodonto_sessions {
  appointment_id            String?
  clinic_id                 String
  consentimento_assinado_em String?
  consentimento_gravacao    Boolean?
  created_at                DateTime @default(now())
  created_by                String
  dentist_id                String
  dentist_joined_at         String?
  diagnostico_preliminar    String?
  duracao_minutos           Int?
  ended_at                  String?
  id                        String   @id
  notas_pos_consulta        String?
  notas_pre_consulta        String?
  patient_id                String
  patient_joined_at         String?
  platform                  String
  prescricoes               Json?
  problemas_tecnicos        String?
  qualidade_audio           String?
  qualidade_video           String?
  recording_url             String?
  room_id                   String?
  room_url                  String?
  scheduled_end             String
  scheduled_start           String
  started_at                String?
  status                    String
  updated_at                DateTime

  @@map("teleodonto_sessions")
  @@schema("public")
}

model public_terminal_command_history {
  clinic_id      String
  command        String
  duration_ms    Int?
  executed_at    String
  exit_code      Int?
  id             String   @id
  output         String?
  user_id        String
  was_successful Boolean?

  @@map("terminal_command_history")
  @@schema("public")
}

model public_tiss_batches {
  batch_number      String
  clinic_id         String
  created_at        DateTime @default(now())
  guide_ids         String[]
  id                String   @id
  insurance_company String
  processed_at      String?
  sent_at           String?
  status            String
  total_amount      Int
  total_guides      Int

  @@map("tiss_batches")
  @@schema("public")
}

model public_tiss_guides {
  amount            Int
  batch_id          String?
  clinic_id         String
  created_at        DateTime @default(now())
  guide_number      String
  id                String   @id
  insurance_company String
  patient_id        String
  procedure_code    String
  procedure_name    String
  response_date     String?
  service_date      String
  status            String
  submission_date   String?
  updated_at        DateTime

  @@map("tiss_guides")
  @@schema("public")
}

model public_triagem_teleconsulta {
  alergias         String?
  created_at       DateTime @default(now())
  fotos_anexas     Json?
  id               String   @id
  intensidade_dor  Int?
  medicamentos_uso String?
  sintomas         String[]
  teleconsulta_id  String
  tempo_sintoma    String?

  @@map("triagem_teleconsulta")
  @@schema("public")
}

model public_user_clinic_access {
  clinic_id  String
  created_at DateTime @default(now())
  id         String   @id
  is_default Boolean?
  user_id    String

  @@map("user_clinic_access")
  @@schema("public")
}

model public_user_module_permissions {
  can_delete        Boolean
  can_edit          Boolean
  can_view          Boolean
  created_at        DateTime @default(now())
  id                String   @id
  module_catalog_id Int
  updated_at        DateTime
  user_id           String

  @@map("user_module_permissions")
  @@schema("public")
}

model public_user_roles {
  created_at DateTime @default(now())
  id         String   @id
  role       String
  user_id    String

  @@map("user_roles")
  @@schema("public")
}

model public_wiki_page_versions {
  change_summary String?
  changed_by     String
  content        String
  created_at     DateTime @default(now())
  id             String   @id
  page_id        String
  title          String
  version        Int

  @@map("wiki_page_versions")
  @@schema("public")
}

model public_wiki_pages {
  category     String
  clinic_id    String
  content      String
  created_at   DateTime @default(now())
  created_by   String
  id           String   @id
  is_published Boolean
  parent_id    String?
  slug         String
  tags         String[]
  title        String
  updated_at   DateTime
  updated_by   String?
  version      Int

  @@map("wiki_pages")
  @@schema("public")
}

model users {
  id                 String    @id @db.Uuid
  email              String?
  encrypted_password String?
  email_confirmed_at DateTime? @db.Timestamptz(6)

  @@schema("auth")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model internalsupabase {
  abuse_type   String
  auto_blocked Boolean?
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  details      Json?
  endpoint     String
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address   String
  resolved     Boolean?
  resolved_at  String?
  resolved_by  String?
  severity     String
  user_id      String?  @db.Uuid

  @@map("__internalsupabase")
  @@schema("tenant_data")
}

model admin_configurations {
  clinic_id   String    @db.Uuid
  config_data Json
  config_type String
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  created_by  String?
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active   Boolean?
  updated_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model analises_radiograficas {
  ai_model_version      String?
  ai_processing_time_ms Decimal? @db.Decimal
  auto_approved         Boolean?
  clinic_id             String   @db.Uuid
  confidence_score      Decimal? @db.Decimal
  created_at            DateTime @default(now()) @db.Timestamptz(6)
  created_by            String
  feedback_comments     String?
  feedback_rating       Decimal? @db.Decimal
  id                    String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  imagem_storage_path   String
  imagem_url            String
  observacoes_dentista  String?
  patient_id            String   @db.Uuid
  problemas_detectados  Decimal? @db.Decimal
  prontuario_id         String?  @db.Uuid
  resultado_ia          Json?
  revisado_em           String?
  revisado_por          String?
  revisado_por_dentista Boolean?
  status_analise        String
  tipo_radiografia      String
  updated_at            DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model analises_radiograficas_history {
  ai_model_version     String?
  analise_id           String   @db.Uuid
  confidence_score     Decimal? @db.Decimal
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  created_by           String
  id                   String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  problemas_detectados Decimal? @db.Decimal
  resultado_ia         Json
  versao               Decimal  @db.Decimal

  @@schema("tenant_data")
}

model appointment_confirmations {
  appointment_id      String   @db.Uuid
  confirmation_method String
  confirmed_at        String?
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  error_message       String?
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  message_content     String?
  phone_number        String?
  sent_at             String?
  status              String

  @@schema("tenant_data")
}

model appointment_reminders {
  appointment_id   String   @db.Uuid
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  error_message    String?
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  message_template String
  phone_number     String?
  reminder_type    String
  scheduled_for    String
  sent_at          String?
  status           String

  @@schema("tenant_data")
}

model appointments {
  clinic_id    String   @db.Uuid
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  created_by   String
  dentist_id   String   @db.Uuid
  description  String?
  end_time     String
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  patient_id   String   @db.Uuid
  start_time   String
  status       String
  title        String
  treatment_id String?  @db.Uuid
  updated_at   DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model architecture_decision_records {
  adr_number              Decimal  @db.Decimal
  alternatives_considered String?
  clinic_id               String   @db.Uuid
  consequences            String
  context                 String
  created_at              DateTime @default(now()) @db.Timestamptz(6)
  created_by              String
  decided_at              String?
  decided_by              String?
  decision                String
  id                      String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  status                  String
  superseded_by_adr_id    String?  @db.Uuid
  supersedes_adr_id       String?  @db.Uuid
  tags                    String[]
  title                   String
  updated_at              DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model audit_logs {
  action           String
  action_type      String?
  affected_records Json?
  clinic_id        String?  @db.Uuid
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  details          Json?
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address       String
  target_module_id String?  @db.Uuid
  user_agent       String?
  user_id          String?  @db.Uuid

  @@schema("tenant_data")
}

model audit_trail {
  action            String
  clinic_id         String? @db.Uuid
  entity_id         String? @db.Uuid
  entity_type       String
  id                String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address        String
  new_values        Json?
  old_values        Json?
  sensitivity_level String
  timestamp         String
  user_agent        String?
  user_id           String? @db.Uuid

  @@schema("tenant_data")
}

model backup_history {
  backup_type            String
  checksum_md5           String?
  checksum_sha256        String?
  clinic_id              String   @db.Uuid
  completed_at           String?
  compression_ratio      Decimal? @db.Decimal
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  created_by             String?
  error_message          String?
  file_path              String?
  file_size_bytes        Decimal? @db.Decimal
  format                 String?
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  includes_postgres_dump Boolean?
  metadata               Json?
  parent_backup_id       String?  @db.Uuid
  restore_tested_at      String?
  retention_policy_id    String?  @db.Uuid
  status                 String
  transfer_speed_mbps    Decimal? @db.Decimal
  verified_at            String?

  @@schema("tenant_data")
}

model backup_replications {
  backup_id          String    @db.Uuid
  checksum_md5       String?
  completed_at       String?
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  error_message      String?
  file_size_bytes    Decimal?  @db.Decimal
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metadata           Json?
  region             String
  replication_status String
  source_clinic_id   String    @db.Uuid
  started_at         String?
  storage_path       String?
  storage_provider   String
  target_clinic_id   String    @db.Uuid
  updated_at         DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model backup_retention_policies {
  auto_delete_enabled Boolean?
  backup_type         String
  clinic_id           String    @db.Uuid
  created_at          DateTime? @default(now()) @db.Timestamptz(6)
  id                  String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  keep_daily          Decimal   @db.Decimal
  keep_monthly        Decimal   @db.Decimal
  keep_weekly         Decimal   @db.Decimal
  keep_yearly         Decimal   @db.Decimal
  name                String
  updated_at          DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model backup_verification_log {
  backup_id         String  @db.Uuid
  details           Json?
  id                String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  status            String
  verification_type String
  verified_at       String?

  @@schema("tenant_data")
}

model bi_dashboards {
  clinic_id                String   @db.Uuid
  created_at               DateTime @default(now()) @db.Timestamptz(6)
  created_by               String
  description              String?
  id                       String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_default               Boolean
  is_public                Boolean
  layout                   Json?
  name                     String
  refresh_interval_minutes Decimal? @db.Decimal
  shared_with              String[]
  tags                     String[]
  updated_at               DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model bi_data_cache {
  cache_key  String
  clinic_id  String   @db.Uuid
  created_at DateTime @default(now()) @db.Timestamptz(6)
  data       Json
  expires_at String
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  widget_id  String?  @db.Uuid

  @@schema("tenant_data")
}

model bi_metrics {
  aggregation_period String
  calculation_type   String
  clinic_id          String   @db.Uuid
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  data_sources       String[]
  description        String?
  formula            String?
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  last_calculated_at String?
  metadata           Json?
  metric_key         String
  name               String
  trend              Decimal? @db.Decimal
  updated_at         DateTime @default(now()) @db.Timestamptz(6)
  value              Decimal? @db.Decimal

  @@schema("tenant_data")
}

model bi_reports {
  clinic_id          String   @db.Uuid
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  created_by         String
  description        String?
  format             String
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active          Boolean
  last_generated_at  String?
  name               String
  next_generation_at String?
  parameters         Json?
  recipients         String[]
  report_type        String
  schedule           Json?
  updated_at         DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model bi_widgets {
  cache_duration_minutes Decimal? @db.Decimal
  chart_type             String?
  clinic_id              String   @db.Uuid
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  dashboard_id           String   @db.Uuid
  data_source            String
  display_config         Json?
  height                 Decimal  @db.Decimal
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                   String
  position_x             Decimal  @db.Decimal
  position_y             Decimal  @db.Decimal
  query_config           Json
  refresh_on_load        Boolean
  updated_at             DateTime @default(now()) @db.Timestamptz(6)
  widget_type            String
  width                  Decimal  @db.Decimal

  @@schema("tenant_data")
}

model blocked_times {
  clinic_id      String    @db.Uuid
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  created_by     String
  dentist_id     String    @db.Uuid
  end_datetime   String
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  reason         String
  start_datetime String

  @@schema("tenant_data")
}

model budget_approvals {
  acao                  String
  alteracoes_realizadas Json?
  budget_id             String   @db.Uuid
  created_at            DateTime @default(now()) @db.Timestamptz(6)
  id                    String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  motivo                String?
  usuario_id            String   @db.Uuid
  valor_orcamento       Decimal  @db.Decimal
  versao                Decimal  @db.Decimal

  @@schema("tenant_data")
}

model budget_items {
  budget_id           String   @db.Uuid
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  dente_regiao        String?
  desconto_percentual Decimal? @db.Decimal
  desconto_valor      Decimal? @db.Decimal
  descricao           String
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  observacoes         String?
  ordem               Decimal  @db.Decimal
  procedimento_id     String?  @db.Uuid
  quantidade          Decimal  @db.Decimal
  valor_total         Decimal  @db.Decimal
  valor_unitario      Decimal  @db.Decimal

  @@schema("tenant_data")
}

model budget_versions {
  budget_id     String   @db.Uuid
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  created_by    String
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  snapshot_data Json
  versao        Decimal  @db.Decimal

  @@schema("tenant_data")
}

model budgets {
  aprovado_em         String?
  aprovado_por        String?
  clinic_id           String   @db.Uuid
  contrato_id         String?  @db.Uuid
  convertido_contrato Boolean?
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  created_by          String
  data_expiracao      String?
  desconto_percentual Decimal? @db.Decimal
  desconto_valor      Decimal? @db.Decimal
  descricao           String?
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  motivo_rejeicao     String?
  numero_orcamento    String
  observacoes         String?
  patient_id          String   @db.Uuid
  rejeitado_em        String?
  rejeitado_por       String?
  status              String
  tipo_plano          String
  titulo              String
  updated_at          DateTime @default(now()) @db.Timestamptz(6)
  validade_dias       Decimal  @db.Decimal
  valor_subtotal      Decimal  @db.Decimal
  valor_total         Decimal  @db.Decimal

  @@schema("tenant_data")
}

model caixa_incidentes {
  boletim_ocorrencia  String?
  clinic_id           String    @db.Uuid
  created_at          DateTime? @default(now()) @db.Timestamptz(6)
  data_incidente      String
  descricao           String?
  dia_semana          Decimal   @db.Decimal
  horario_incidente   String
  id                  String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metadata            Json?
  tipo_incidente      String
  valor_caixa_momento Decimal?  @db.Decimal
  valor_perdido       Decimal?  @db.Decimal

  @@schema("tenant_data")
}

model caixa_movimentos {
  aberto_em       String?
  caixa_id        String?   @db.Uuid
  clinic_id       String    @db.Uuid
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  created_by      String
  diferenca       Decimal?  @db.Decimal
  fechado_em      String?
  horario_risco   String?
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  motivo_sangria  String?
  observacoes     String?
  risco_calculado Decimal?  @db.Decimal
  status          String
  sugerido_por_ia Boolean?
  tipo            String
  valor           Decimal   @db.Decimal
  valor_esperado  Decimal?  @db.Decimal
  valor_final     Decimal?  @db.Decimal
  valor_inicial   Decimal?  @db.Decimal

  @@schema("tenant_data")
}

model campaign_metrics {
  bounce_rate       Decimal? @db.Decimal
  campaign_id       String   @db.Uuid
  click_rate        Decimal? @db.Decimal
  conversion_rate   Decimal? @db.Decimal
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metric_date       String
  open_rate         Decimal? @db.Decimal
  revenue_generated Decimal? @db.Decimal
  total_clicked     Decimal? @db.Decimal
  total_converted   Decimal? @db.Decimal
  total_delivered   Decimal? @db.Decimal
  total_errors      Decimal? @db.Decimal
  total_opened      Decimal? @db.Decimal
  total_sent        Decimal? @db.Decimal
  unsubscribe_count Decimal? @db.Decimal
  updated_at        DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model campaign_sends {
  campaign_id       String   @db.Uuid
  clicked_at        String?
  converted_at      String?
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  delivered_at      String?
  error_code        String?
  error_message     String?
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  message_content   String?
  metadata          Json?
  opened_at         String?
  patient_id        String   @db.Uuid
  recipient_contact String
  recipient_name    String
  retry_count       Decimal? @db.Decimal
  scheduled_for     String
  sent_at           String?
  status            String

  @@schema("tenant_data")
}

model campaign_templates {
  category      String?
  clinic_id     String   @db.Uuid
  content       String
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  created_by    String
  description   String?
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_default    Boolean?
  name          String
  subject       String?
  template_type String
  updated_at    DateTime @default(now()) @db.Timestamptz(6)
  variables     Json?

  @@schema("tenant_data")
}

model campaign_triggers {
  campaign_id       String   @db.Uuid
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  delay_days        Decimal? @db.Decimal
  delay_hours       Decimal? @db.Decimal
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active         Boolean
  trigger_condition Json
  trigger_type      String

  @@schema("tenant_data")
}

model campanha_envios {
  aberto_em         String?
  campanha_id       String   @db.Uuid
  clicado_em        String?
  convertido_em     String?
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  destinatario_id   String   @db.Uuid
  destinatario_tipo String
  email             String?
  enviado_em        String?
  erro_mensagem     String?
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  status_envio      String
  telefone          String?

  @@schema("tenant_data")
}

model campanhas_inadimplencia {
  clinic_id         String   @db.Uuid
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  data_envio        String?
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  inadimplente_id   String?  @db.Uuid
  mensagem_enviada  String?
  resposta_recebida Boolean?
  status            String
  tipo_campanha     String
  updated_at        DateTime @default(now()) @db.Timestamptz(6)
  valor_recuperado  Decimal? @db.Decimal

  @@schema("tenant_data")
}

model campanhas_marketing {
  clinic_id           String   @db.Uuid
  conteudo_template   String
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  created_by          String
  data_fim            String?
  data_inicio         String?
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nome                String
  segmento_alvo       Json?
  status              String
  taxa_abertura       Decimal? @db.Decimal
  taxa_clique         Decimal? @db.Decimal
  taxa_conversao      Decimal? @db.Decimal
  tipo                String
  total_aberturas     Decimal? @db.Decimal
  total_cliques       Decimal? @db.Decimal
  total_conversoes    Decimal? @db.Decimal
  total_destinatarios Decimal? @db.Decimal
  total_enviados      Decimal? @db.Decimal
  updated_at          DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model clinic_modules {
  clinic_id         String   @db.Uuid
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active         Boolean
  module_catalog_id String   @db.Uuid
  subscribed_at     String
  updated_at        DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model clinics {
  auto_cleanup_enabled  Boolean?
  backup_retention_days Decimal? @db.Decimal
  created_at            DateTime @default(now()) @db.Timestamptz(6)
  id                    String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                  String
  updated_at            DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model cloud_storage_configs {
  clinic_id  String   @db.Uuid
  config     Json
  created_at DateTime @default(now()) @db.Timestamptz(6)
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active  Boolean
  provider   String
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model collection_actions {
  action_date        String
  action_type        String
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  created_by         String
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  message_content    String?
  overdue_account_id String   @db.Uuid
  response_received  String?
  scheduled_for      String?
  status             String

  @@schema("tenant_data")
}

model collection_automation_config {
  action_type      String
  clinic_id        String   @db.Uuid
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  created_by       String
  days_trigger     Decimal  @db.Decimal
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active        Boolean
  message_template String
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model contas_pagar {
  anexo_url       String?
  categoria       String
  clinic_id       String   @db.Uuid
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  created_by      String?
  data_emissao    String
  data_pagamento  String?
  data_vencimento String
  descricao       String
  forma_pagamento String?
  fornecedor      String
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  observacoes     String?
  parcela_numero  Decimal? @db.Decimal
  parcela_total   Decimal? @db.Decimal
  periodicidade   String?
  recorrente      Boolean?
  status          String
  updated_at      DateTime @default(now()) @db.Timestamptz(6)
  valor           Decimal  @db.Decimal
  valor_pago      Decimal? @db.Decimal

  @@schema("tenant_data")
}

model contas_receber {
  clinic_id       String   @db.Uuid
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  created_by      String?
  data_emissao    String
  data_pagamento  String?
  data_vencimento String
  descricao       String
  forma_pagamento String?
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  observacoes     String?
  parcela_numero  Decimal? @db.Decimal
  parcela_total   Decimal? @db.Decimal
  patient_id      String?  @db.Uuid
  status          String
  updated_at      DateTime @default(now()) @db.Timestamptz(6)
  valor           Decimal  @db.Decimal
  valor_pago      Decimal? @db.Decimal

  @@schema("tenant_data")
}

model contrato_anexos {
  caminho_storage String
  contrato_id     String   @db.Uuid
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mime_type       String
  nome_arquivo    String
  tamanho_bytes   Decimal  @db.Decimal
  uploaded_by     String

  @@schema("tenant_data")
}

model contrato_templates {
  ativo                 Boolean
  clinic_id             String   @db.Uuid
  conteudo_html         String
  created_at            DateTime @default(now()) @db.Timestamptz(6)
  created_by            String
  id                    String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nome                  String
  tipo_tratamento       String
  updated_at            DateTime @default(now()) @db.Timestamptz(6)
  variaveis_disponiveis Json?

  @@schema("tenant_data")
}

model contratos {
  assinado_em                String?
  assinatura_dentista_base64 String?
  assinatura_paciente_base64 String?
  cancelado_em               String?
  clinic_id                  String   @db.Uuid
  conteudo_html              String
  created_at                 DateTime @default(now()) @db.Timestamptz(6)
  created_by                 String
  data_inicio                String
  data_termino               String?
  hash_blockchain            String?
  id                         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_assinatura              String?
  motivo_cancelamento        String?
  numero_contrato            String
  orcamento_id               String?  @db.Uuid
  patient_id                 String   @db.Uuid
  renovacao_automatica       Boolean?
  status                     String
  template_id                String?  @db.Uuid
  titulo                     String
  updated_at                 DateTime @default(now()) @db.Timestamptz(6)
  valor_contrato             Decimal  @db.Decimal

  @@schema("tenant_data")
}

model crm_activities {
  activity_type  String
  assigned_to    String
  clinic_id      String   @db.Uuid
  completed_date String?
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  description    String?
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lead_id        String   @db.Uuid
  outcome        String?
  scheduled_date String?
  status         String
  title          String
  updated_at     DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model crm_conversions {
  assigned_to          String?
  clinic_id            String   @db.Uuid
  conversion_type      String
  conversion_value     Decimal? @db.Decimal
  converted_at         String
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  id                   String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lead_id              String   @db.Uuid
  time_to_convert_days Decimal? @db.Decimal
  total_interactions   Decimal? @db.Decimal

  @@schema("tenant_data")
}

model crm_interactions {
  attachments      Json?
  clinic_id        String   @db.Uuid
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  created_by       String
  description      String
  duration_minutes Decimal? @db.Decimal
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  interaction_type String
  lead_id          String   @db.Uuid
  next_action      String?
  next_action_date String?
  outcome          String?
  subject          String?

  @@schema("tenant_data")
}

model crm_leads {
  assigned_to          String?
  clinic_id            String   @db.Uuid
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  created_by           String
  email                String?
  estimated_value      Decimal? @db.Decimal
  id                   String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  interest_description String?
  name                 String
  next_contact_date    String?
  notes                String?
  phone                String?
  source               String
  status               String
  tags                 String[]
  updated_at           DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model crm_stages {
  average_time_days Decimal? @db.Decimal
  clinic_id         String   @db.Uuid
  color             String?
  conversion_rate   Decimal? @db.Decimal
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  description       String?
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active         Boolean
  name              String
  order_index       Decimal  @db.Decimal
  updated_at        DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model crypto_candlestick_data {
  close_price Decimal  @db.Decimal
  close_time  String
  coin_type   String
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  high_price  Decimal  @db.Decimal
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  interval    String
  low_price   Decimal  @db.Decimal
  open_price  Decimal  @db.Decimal
  open_time   String
  volume      Decimal  @db.Decimal

  @@schema("tenant_data")
}

model crypto_payments {
  amount_brl      Decimal   @db.Decimal
  checkout_link   String?
  clinic_id       String    @db.Uuid
  confirmations   Decimal?  @db.Decimal
  confirmed_at    String?
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  created_by      String?
  crypto_amount   Decimal?  @db.Decimal
  crypto_currency String?
  currency        String
  expires_at      String?
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  invoice_id      String    @db.Uuid
  metadata        Json?
  order_id        String    @db.Uuid
  qr_code_data    String?
  status          String
  transaction_id  String?   @db.Uuid
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model crypto_price_alerts {
  alert_type              String
  auto_convert_on_trigger Boolean?
  cascade_enabled         Boolean?
  cascade_group_id        String?  @db.Uuid
  cascade_order           Decimal? @db.Decimal
  clinic_id               String   @db.Uuid
  coin_type               String
  conversion_percentage   Decimal? @db.Decimal
  created_at              DateTime @default(now()) @db.Timestamptz(6)
  created_by              String
  id                      String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active               Boolean
  last_triggered_at       String?
  notification_method     String[]
  stop_loss_enabled       Boolean?
  target_rate_brl         Decimal  @db.Decimal
  updated_at              DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model dentist_schedules {
  clinic_id   String   @db.Uuid
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  created_by  String
  day_of_week Decimal  @db.Decimal
  dentist_id  String   @db.Uuid
  end_time    String
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active   Boolean
  start_time  String
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model estoque_pedidos {
  clinic_id              String   @db.Uuid
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  created_by             String
  data_pedido            String
  data_prevista_entrega  String?
  data_recebimento       String?
  fornecedor_id          String   @db.Uuid
  gerado_automaticamente Boolean?
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  numero_pedido          String
  observacoes            String?
  status                 String
  tipo                   String
  updated_at             DateTime @default(now()) @db.Timestamptz(6)
  valor_total            Decimal? @db.Decimal

  @@schema("tenant_data")
}

model estoque_pedidos_config {
  clinic_id              String   @db.Uuid
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  dias_entrega_estimados Decimal? @db.Decimal
  gerar_automaticamente  Boolean?
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ponto_pedido           Decimal  @db.Decimal
  produto_id             String   @db.Uuid
  quantidade_reposicao   Decimal  @db.Decimal
  updated_at             DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model estoque_pedidos_itens {
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  observacoes         String?
  pedido_id           String   @db.Uuid
  preco_unitario      Decimal  @db.Decimal
  produto_id          String   @db.Uuid
  quantidade          Decimal  @db.Decimal
  quantidade_recebida Decimal? @db.Decimal
  valor_total         Decimal  @db.Decimal

  @@schema("tenant_data")
}

model fechamento_caixa {
  arquivo_sped_gerado_em String?
  arquivo_sped_path      String?
  caixa_movimento_id     String    @db.Uuid
  clinic_id              String    @db.Uuid
  created_at             DateTime? @default(now()) @db.Timestamptz(6)
  created_by             String
  data_fechamento        String
  divergencia            Decimal   @db.Decimal
  id                     String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  observacoes            String?
  percentual_divergencia Decimal?  @db.Decimal
  quantidade_nfce        Decimal   @db.Decimal
  quantidade_vendas_pdv  Decimal   @db.Decimal
  total_nfce_emitidas    Decimal   @db.Decimal
  total_sangrias         Decimal   @db.Decimal
  total_suprimentos      Decimal   @db.Decimal
  total_vendas_pdv       Decimal   @db.Decimal
  vendas_sem_nfce        Decimal   @db.Decimal

  @@schema("tenant_data")
}

model financial_categories {
  active     Boolean?
  clinic_id  String   @db.Uuid
  color      String?
  created_at DateTime @default(now()) @db.Timestamptz(6)
  icon       String?
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name       String
  type       String
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model financial_transactions {
  amount           Decimal  @db.Decimal
  category         String
  clinic_id        String   @db.Uuid
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  created_by       String?
  description      String
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  notes            String?
  payment_method   String?
  status           String
  tags             String[]
  transaction_date String
  type             String
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model fiscal_config {
  ambiente                 String
  certificado_digital      String?
  clinic_id                String    @db.Uuid
  cnpj                     String
  codigo_regime_tributario Decimal?  @db.Decimal
  contingencia_enabled     Boolean?
  contingencia_motivo      String?
  created_at               DateTime? @default(now()) @db.Timestamptz(6)
  csc_id                   String?   @db.Uuid
  csc_token                String?
  email_contabilidade      String?
  endereco                 Json?
  id                       String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  inscricao_estadual       String?
  is_active                Boolean?
  nome_fantasia            String?
  numero_ultimo_nfce       Decimal?  @db.Decimal
  razao_social             String
  regime_tributario        String
  senha_certificado        String?
  serie_nfce               Decimal?  @db.Decimal
  tipo_emissao             String
  updated_at               DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model funcionarios {
  avatar_url       String?
  cargo            String
  celular          String
  clinic_id        String   @db.Uuid
  cpf              String
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  data_admissao    String
  data_nascimento  String
  dias_trabalho    Decimal  @db.Decimal
  email            String
  endereco         Json
  horario_trabalho Json
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nome             String
  observacoes      String?
  permissoes       Json
  rg               String?
  salario          Decimal  @db.Decimal
  sexo             String
  status           String
  telefone         String
  updated_at       DateTime @default(now()) @db.Timestamptz(6)
  user_id          String?  @db.Uuid

  @@schema("tenant_data")
}

model github_events {
  clinic_id    String   @db.Uuid
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  event_data   Json
  event_type   String
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  triggered_by String?

  @@schema("tenant_data")
}

model historico_clinico {
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  created_by         String
  dados_estruturados Json?
  descricao          String
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  prontuario_id      String   @db.Uuid
  tipo               String
  titulo             String
  updated_at         DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model inadimplentes {
  clinic_id          String   @db.Uuid
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  dias_atraso        Decimal  @db.Decimal
  email              String?
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nome_paciente      String
  observacoes        String?
  patient_id         String   @db.Uuid
  status             String
  telefone           String?
  ultima_cobranca    String?
  updated_at         DateTime @default(now()) @db.Timestamptz(6)
  valor_total_devido Decimal  @db.Decimal

  @@schema("tenant_data")
}

model integracao_contabil_config {
  api_key             String?
  api_secret          String?
  api_url             String
  ativo               Boolean
  clinic_id           String   @db.Uuid
  codigo_empresa      String?
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  email_contador      String?
  enviar_nfce_dados   Boolean
  enviar_sped_fiscal  Boolean
  envio_automatico    Boolean
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metadata            Json?
  periodicidade_envio String
  software            String
  updated_at          DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model integracao_contabil_envios {
  arquivo_path       String?
  arquivo_size_bytes Decimal? @db.Decimal
  clinic_id          String   @db.Uuid
  config_id          String   @db.Uuid
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  enviado_em         String?
  erro_mensagem      String?
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  max_tentativas     Decimal  @db.Decimal
  periodo_referencia String
  response_data      Json?
  status             String
  tentativas         Decimal  @db.Decimal
  tipo_documento     String
  updated_at         DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model inventario_agendamentos {
  ativo                 Boolean
  categorias_ids        String[]
  clinic_id             String   @db.Uuid
  created_at            DateTime @default(now()) @db.Timestamptz(6)
  created_by            String
  dia_execucao          Decimal? @db.Decimal
  dia_semana            Decimal? @db.Decimal
  id                    String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nome                  String
  notificar_dias_antes  Decimal? @db.Decimal
  notificar_responsavel Boolean?
  observacoes           String?
  periodicidade         String
  proxima_execucao      String?
  responsavel           String
  tipo_inventario       String
  ultima_execucao       String?
  updated_at            DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model inventario_itens {
  contado_em             String?
  contado_por            String?
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  divergencia            Decimal? @db.Decimal
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  inventario_id          String   @db.Uuid
  lote                   String?
  observacoes            String?
  percentual_divergencia Decimal? @db.Decimal
  produto_id             String   @db.Uuid
  produto_nome           String
  quantidade_fisica      Decimal? @db.Decimal
  quantidade_sistema     Decimal  @db.Decimal
  valor_divergencia      Decimal? @db.Decimal
  valor_unitario         Decimal  @db.Decimal

  @@schema("tenant_data")
}

model inventarios {
  clinic_id                String   @db.Uuid
  created_at               DateTime @default(now()) @db.Timestamptz(6)
  created_by               String
  data                     String
  divergencias_encontradas Decimal? @db.Decimal
  id                       String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  itens_contados           Decimal? @db.Decimal
  numero                   String
  observacoes              String?
  responsavel              String
  status                   String
  tipo                     String
  total_itens              Decimal? @db.Decimal
  updated_at               DateTime @default(now()) @db.Timestamptz(6)
  valor_divergencias       Decimal? @db.Decimal

  @@schema("tenant_data")
}

model lead_interacoes {
  agendou_avaliacao Boolean?
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  created_by        String
  descricao         String
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lead_id           String   @db.Uuid
  proximo_passo     String?
  resultado         String?
  tipo              String

  @@schema("tenant_data")
}

model lead_tags {
  created_at DateTime @default(now()) @db.Timestamptz(6)
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lead_id    String   @db.Uuid
  tag        String

  @@schema("tenant_data")
}

model leads {
  atribuido_a      String?
  clinic_id        String   @db.Uuid
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  created_by       String?
  email            String?
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  interesse        String?
  motivo_perda     String?
  nome             String
  observacoes      String?
  origem           String
  proximo_followup String?
  score_qualidade  Decimal? @db.Decimal
  status_funil     String
  telefone         String?
  temperatura      String
  ultimo_contato   String?
  updated_at       DateTime @default(now()) @db.Timestamptz(6)
  utm_campaign     String?
  utm_medium       String?
  utm_source       String?
  valor_estimado   Decimal? @db.Decimal
  whatsapp         String?

  @@schema("tenant_data")
}

model lgpd_consents {
  accepted     Boolean
  accepted_at  String?
  accepted_by  String?
  clinic_id    String   @db.Uuid
  consent_text String
  consent_type String
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  expires_at   String?
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address   String?
  metadata     Json?
  patient_id   String   @db.Uuid
  revoked      Boolean?
  revoked_at   String?
  revoked_by   String?
  updated_at   DateTime @default(now()) @db.Timestamptz(6)
  user_agent   String?
  version      Decimal  @db.Decimal

  @@schema("tenant_data")
}

model lgpd_data_consents {
  clinic_id    String   @db.Uuid
  consent_type String
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  expires_at   String?
  granted      Boolean
  granted_at   String?
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  patient_id   String   @db.Uuid
  revoked_at   String?

  @@schema("tenant_data")
}

model lgpd_data_exports {
  clinic_id       String   @db.Uuid
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  download_count  Decimal? @db.Decimal
  downloaded_at   String?
  error_message   String?
  expires_at      String?
  export_type     String
  file_format     String?
  file_path       String?
  file_size_bytes Decimal? @db.Decimal
  generated_at    String?
  generated_by    String
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metadata        Json?
  patient_id      String   @db.Uuid
  request_id      String?  @db.Uuid
  status          String
  tables_included String[]
  updated_at      DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model lgpd_data_requests {
  clinic_id        String   @db.Uuid
  completed_at     String?
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  data_export_id   String?  @db.Uuid
  description      String?
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metadata         Json?
  patient_id       String   @db.Uuid
  rejection_reason String?
  request_type     String
  requested_at     String
  requested_by     String
  responded_at     String?
  responded_by     String?
  response         String?
  status           String
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model login_attempts {
  attempted_at             String
  created_at               DateTime @default(now()) @db.Timestamptz(6)
  device_fingerprint       String?
  email                    String
  id                       String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address               String?
  ip_geolocation           Json?
  session_duration_seconds Decimal? @db.Decimal
  success                  Boolean
  user_agent               String?

  @@schema("tenant_data")
}

model marketing_campaigns {
  campaign_type    String
  channel          String
  clinic_id        String   @db.Uuid
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  created_by       String
  description      String?
  end_date         String?
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  last_sent_at     String?
  metadata         Json?
  name             String
  schedule_config  Json?
  send_immediately Boolean?
  start_date       String?
  status           String
  target_audience  Json?
  template_id      String?  @db.Uuid
  trigger_config   Json?
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model module_catalog {
  category    String
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  description String?
  icon        String?
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  module_key  String
  name        String

  @@schema("tenant_data")
}

model module_configuration_templates {
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  description String?
  icon        String?
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active   Boolean
  modules     Json
  name        String
  specialty   String
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model module_dependencies {
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  depends_on_module_id String   @db.Uuid
  id                   String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  module_id            String   @db.Uuid

  @@schema("tenant_data")
}

model movimentacoes_estoque {
  clinic_id           String   @db.Uuid
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  motivo              String?
  observacoes         String?
  produto_id          String   @db.Uuid
  quantidade          Decimal  @db.Decimal
  quantidade_anterior Decimal  @db.Decimal
  quantidade_atual    Decimal  @db.Decimal
  tipo                String
  usuario_id          String   @db.Uuid
  valor_total         Decimal  @db.Decimal
  valor_unitario      Decimal  @db.Decimal

  @@schema("tenant_data")
}

model nfce_carta_correcao {
  clinic_id     String    @db.Uuid
  codigo_status String?
  correcao      String
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  created_by    String
  data_evento   String?
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  motivo        String?
  nfce_id       String    @db.Uuid
  protocolo     String?
  sequencia     Decimal   @db.Decimal
  status        String
  xml_evento    String?

  @@schema("tenant_data")
}

model nfce_contingencia {
  chave_acesso             String
  clinic_id                String   @db.Uuid
  created_at               DateTime @default(now()) @db.Timestamptz(6)
  emitido_em               String
  erro_sincronizacao       String?
  forma_pagamento          String
  id                       String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  itens                    Json
  modo_contingencia        String
  motivo_contingencia      String
  numero_nfce              Decimal  @db.Decimal
  protocolo_autorizacao    String?
  serie                    Decimal  @db.Decimal
  sincronizado_em          String?
  status_sincronizacao     String
  tentativas_sincronizacao Decimal  @db.Decimal
  updated_at               DateTime @default(now()) @db.Timestamptz(6)
  valor_total              Decimal  @db.Decimal
  venda_id                 String?  @db.Uuid
  xml_nfce                 String

  @@schema("tenant_data")
}

model nfce_emitidas {
  ambiente              String
  chave_acesso          String
  clinic_id             String    @db.Uuid
  contingencia          Boolean?
  created_at            DateTime? @default(now()) @db.Timestamptz(6)
  data_cancelamento     String?
  data_emissao          String?
  error_message         String?
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metadata              Json?
  motivo_cancelamento   String?
  numero_nfce           Decimal   @db.Decimal
  pdf_url               String?
  protocolo_autorizacao String?
  qrcode_url            String?
  serie                 Decimal   @db.Decimal
  status                String
  tipo_emissao          String
  updated_at            DateTime? @default(now()) @db.Timestamptz(6)
  valor_total           Decimal   @db.Decimal
  venda_id              String    @db.Uuid
  xml_cancelamento      String?
  xml_nfce              String?

  @@schema("tenant_data")
}

model nfce_inutilizacao {
  ano               Decimal   @db.Decimal
  clinic_id         String    @db.Uuid
  codigo_status     String?
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  created_by        String
  data_inutilizacao String?
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  justificativa     String
  motivo            String?
  numero_final      Decimal   @db.Decimal
  numero_inicial    Decimal   @db.Decimal
  protocolo         String?
  serie             Decimal   @db.Decimal
  status            String
  xml_inutilizacao  String?

  @@schema("tenant_data")
}

model notas_fiscais {
  chave_acesso      String?
  clinic_id         String   @db.Uuid
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  created_by        String?
  data_emissao      String
  data_recebimento  String?
  destinatario_cnpj String?
  destinatario_nome String?
  emitente_cnpj     String
  emitente_nome     String
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  numero            String
  observacoes       String?
  pdf_url           String?
  serie             String?
  status            String
  tipo              String
  updated_at        DateTime @default(now()) @db.Timestamptz(6)
  valor_icms        Decimal? @db.Decimal
  valor_iss         Decimal? @db.Decimal
  valor_total       Decimal  @db.Decimal
  xml_url           String?

  @@schema("tenant_data")
}

model notifications {
  clinic_id  String   @db.Uuid
  created_at DateTime @default(now()) @db.Timestamptz(6)
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lida       Boolean
  lida_em    String?
  link_acao  String?
  mensagem   String
  tipo       String
  titulo     String
  user_id    String?  @db.Uuid

  @@schema("tenant_data")
}

model odontogramas {
  clinic_id     String   @db.Uuid
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  history       Json
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  last_updated  String
  prontuario_id String   @db.Uuid
  teeth         Json
  updated_at    DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model onboarding_analytics {
  clinic_id          String   @db.Uuid
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  event_type         String
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metadata           Json?
  step_name          String?
  step_number        Decimal? @db.Decimal
  time_spent_seconds Decimal? @db.Decimal
  user_id            String   @db.Uuid

  @@schema("tenant_data")
}

model orcamento_itens {
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  dente_codigo    String?
  descricao       String
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  observacoes     String?
  orcamento_id    String   @db.Uuid
  ordem           Decimal  @db.Decimal
  procedimento_id String?  @db.Uuid
  quantidade      Decimal  @db.Decimal
  valor_total     Decimal  @db.Decimal
  valor_unitario  Decimal  @db.Decimal

  @@schema("tenant_data")
}

model orcamento_pagamento {
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  forma_pagamento String[]
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  numero_parcelas Decimal? @db.Decimal
  observacoes     String?
  orcamento_id    String   @db.Uuid
  tipo_pagamento  String
  valor_entrada   Decimal? @db.Decimal
  valor_parcela   Decimal? @db.Decimal

  @@schema("tenant_data")
}

model orcamento_visualizacoes {
  duracao_segundos Decimal? @db.Decimal
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address       String?
  orcamento_id     String   @db.Uuid
  user_agent       String?
  visualizado_em   String

  @@schema("tenant_data")
}

model orcamentos {
  aprovado_em         String?
  aprovado_por        String?
  clinic_id           String   @db.Uuid
  convertido_em       String?
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  created_by          String
  data_validade       String
  desconto_percentual Decimal? @db.Decimal
  desconto_valor      Decimal? @db.Decimal
  descricao           String?
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  motivo_rejeicao     String?
  numero_orcamento    String
  observacoes         String?
  patient_id          String   @db.Uuid
  prontuario_id       String?  @db.Uuid
  rejeitado_em        String?
  status              String
  tipo_plano          String
  titulo              String
  updated_at          DateTime @default(now()) @db.Timestamptz(6)
  validade_dias       Decimal  @db.Decimal
  valor_final         Decimal  @db.Decimal
  valor_total         Decimal  @db.Decimal

  @@schema("tenant_data")
}

model overdue_accounts {
  clinic_id        String   @db.Uuid
  conta_receber_id String   @db.Uuid
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  days_overdue     Decimal  @db.Decimal
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  interest_amount  Decimal? @db.Decimal
  original_amount  Decimal  @db.Decimal
  patient_id       String   @db.Uuid
  penalty_amount   Decimal? @db.Decimal
  remaining_amount Decimal  @db.Decimal
  risk_level       String
  status           String
  total_amount     Decimal  @db.Decimal
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model patient_accounts {
  ativo             Boolean
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  email             String
  email_verificado  Boolean
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  patient_id        String   @db.Uuid
  senha_hash        String
  token_verificacao String?
  ultimo_acesso     String?
  updated_at        DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model patient_messages {
  anexos         Json?
  clinic_id      String   @db.Uuid
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lida           Boolean
  lida_em        String?
  mensagem       String
  patient_id     String   @db.Uuid
  remetente_id   String   @db.Uuid
  remetente_tipo String

  @@schema("tenant_data")
}

model patient_notifications {
  created_at DateTime @default(now()) @db.Timestamptz(6)
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lida       Boolean
  lida_em    String?
  link_acao  String?
  mensagem   String
  patient_id String   @db.Uuid
  tipo       String
  titulo     String

  @@schema("tenant_data")
}

model patient_preferences {
  id                      String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  idioma                  String?
  lembrete_consulta_horas Decimal? @db.Decimal
  notificacoes_email      Boolean?
  notificacoes_push       Boolean?
  notificacoes_sms        Boolean?
  notificacoes_whatsapp   Boolean?
  patient_id              String   @db.Uuid
  tema                    String?
  updated_at              DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model patient_sessions {
  created_at DateTime @default(now()) @db.Timestamptz(6)
  expires_at String
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  patient_id String   @db.Uuid
  token      String

  @@schema("tenant_data")
}

model patient_status_history {
  changed_at  String?
  changed_by  String?
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  from_status String?
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  notes       String?
  patient_id  String    @db.Uuid
  reason      String?
  to_status   String

  @@schema("tenant_data")
}

model patients {
  address_city                   String?
  address_complement             String?
  address_country                String?
  address_neighborhood           String?
  address_number                 String?
  address_state                  String?
  address_street                 String?
  address_zipcode                String?
  alcohol_frequency              String?
  allergies_list                 String[]
  birth_date                     String
  bleeding_disorder_details      String?
  blood_pressure_diastolic       Decimal? @db.Decimal
  blood_pressure_systolic        Decimal? @db.Decimal
  blood_type                     String?
  bmi                            Decimal? @db.Decimal
  campanha_origem_id             String?  @db.Uuid
  campanha_origem_nome           String?
  canal_captacao                 String?
  cardiovascular_details         String?
  cargo                          String?
  churn_risk_score               Decimal? @db.Decimal
  clinic_id                      String   @db.Uuid
  clinical_observations          String?
  cnpj_empresa                   String?
  cpf                            String?
  created_at                     DateTime @default(now()) @db.Timestamptz(6)
  created_by                     String?
  current_medications            String[]
  data_conversao                 String?
  data_primeiro_contato          String?
  data_qualificacao              String?
  data_sharing_consent           Boolean?
  diabetes_controlled            Boolean?
  diabetes_type                  String?
  education_level                String?
  email                          String?
  emergency_contact_name         String?
  emergency_contact_relationship String?
  empresa                        String?
  evento_captacao                String?
  first_appointment_date         String?
  full_name                      String
  gender                         String?
  gum_condition                  String?
  has_alcohol_habit              Boolean?
  has_allergies                  Boolean?
  has_bleeding_disorder          Boolean?
  has_cardiovascular_disease     Boolean?
  has_diabetes                   Boolean?
  has_hepatitis                  Boolean?
  has_hiv                        Boolean?
  has_hypertension               Boolean?
  has_medication_allergy         Boolean?
  has_smoking_habit              Boolean?
  has_systemic_disease           Boolean?
  heart_rate                     Decimal? @db.Decimal
  height_cm                      Decimal? @db.Decimal
  hepatitis_type                 String?
  hypertension_controlled        Boolean?
  id                             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  image_usage_consent            Boolean?
  indicado_por                   String?
  indicado_por_dentista_id       String?  @db.Uuid
  indicado_por_paciente_id       String?  @db.Uuid
  is_breastfeeding               Boolean?
  is_pregnant                    Boolean?
  last_appointment_date          String?
  lgpd_consent                   Boolean?
  lgpd_consent_date              String?
  main_complaint                 String?
  marital_status                 String?
  marketing_campaign             String?
  marketing_event                String?
  marketing_promoter             String?
  marketing_source               String?
  marketing_telemarketing_agent  String?
  medication_allergies           String[]
  nationality                    String?
  occupation                     String?
  oral_hygiene_quality           String?
  origem_lead                    String?
  pain_level                     Decimal? @db.Decimal
  patient_code                   String?
  payment_status                 String?
  phone_emergency                String?
  phone_primary                  String
  phone_secondary                String?
  preferred_payment_method       String?
  pregnancy_trimester            Decimal? @db.Decimal
  promotor_id                    String?  @db.Uuid
  promotor_nome                  String?
  propensao_indicacao            Decimal? @db.Decimal
  referral_source                String?
  rg                             String?
  risk_level                     String?
  risk_score_anesthetic          Decimal? @db.Decimal
  risk_score_medical             Decimal? @db.Decimal
  risk_score_overall             Decimal? @db.Decimal
  risk_score_surgical            Decimal? @db.Decimal
  smoking_frequency              String?
  social_name                    String?
  status                         String?
  systemic_diseases              String[]
  total_appointments             Decimal? @db.Decimal
  total_debt                     Decimal? @db.Decimal
  total_paid                     Decimal? @db.Decimal
  treatment_consent              Boolean?
  updated_at                     DateTime @default(now()) @db.Timestamptz(6)
  updated_by                     String?
  valor_lifetime                 Decimal? @db.Decimal
  valor_ticket_medio             Decimal? @db.Decimal
  weight_kg                      Decimal? @db.Decimal

  @@schema("tenant_data")
}

model payment_methods {
  active          Boolean?
  clinic_id       String   @db.Uuid
  config          Json?
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name            String
  taxa_fixa       Decimal? @db.Decimal
  taxa_percentual Decimal? @db.Decimal
  type            String
  updated_at      DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model payment_negotiations {
  accepted_at         String?
  clinic_id           String   @db.Uuid
  completed_at        String?
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  created_by          String
  discount_amount     Decimal? @db.Decimal
  discount_percentage Decimal? @db.Decimal
  first_payment_date  String?
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  installments        Decimal? @db.Decimal
  negotiated_amount   Decimal  @db.Decimal
  original_amount     Decimal  @db.Decimal
  overdue_account_id  String   @db.Uuid
  patient_id          String   @db.Uuid
  status              String
  updated_at          DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model pdv_produtos {
  cest              String?
  cfop              String?
  clinic_id         String    @db.Uuid
  codigo            String
  cofins_aliquota   Decimal?  @db.Decimal
  controla_estoque  Boolean?
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  cst_cofins        String?
  cst_icms          String?
  cst_pis           String?
  descricao         String
  estoque_atual     Decimal?  @db.Decimal
  estoque_minimo    Decimal?  @db.Decimal
  icms_aliquota     Decimal?  @db.Decimal
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active         Boolean?
  ncm               String?
  origem_mercadoria Decimal?  @db.Decimal
  pis_aliquota      Decimal?  @db.Decimal
  tipo              String
  unidade_medida    String?
  updated_at        DateTime? @default(now()) @db.Timestamptz(6)
  valor_unitario    Decimal   @db.Decimal

  @@schema("tenant_data")
}

model pdv_vendas {
  caixa_movimento_id String?  @db.Uuid
  clinic_id          String   @db.Uuid
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  forma_pagamento    String
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metadata           Json?
  numero_venda       String
  observacoes        String?
  status             String
  updated_at         DateTime @default(now()) @db.Timestamptz(6)
  valor_total        Decimal  @db.Decimal

  @@schema("tenant_data")
}

model pep_anexos {
  caminho_storage String
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  descricao       String?
  historico_id    String?  @db.Uuid
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mime_type       String
  nome_arquivo    String
  prontuario_id   String   @db.Uuid
  tamanho_bytes   Decimal  @db.Decimal
  tipo_arquivo    String
  uploaded_by     String

  @@schema("tenant_data")
}

model pep_assinaturas {
  assinatura_base64 String
  historico_id      String? @db.Uuid
  id                String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address        String?
  prontuario_id     String  @db.Uuid
  signed_at         String
  signed_by         String
  tipo_documento    String
  user_agent        String?

  @@schema("tenant_data")
}

model pep_evolucoes {
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  created_by    String
  data_evolucao String
  descricao     String
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tipo          String
  tratamento_id String   @db.Uuid

  @@schema("tenant_data")
}

model pep_odontograma {
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  created_by     String
  dente_codigo   String
  faces_afetadas String[]
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  observacoes    String?
  prontuario_id  String   @db.Uuid
  status         String
  updated_at     DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model pep_odontograma_data {
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  created_by    String
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  notes         String?
  prontuario_id String   @db.Uuid
  status        String
  tooth_number  Decimal  @db.Decimal
  updated_at    DateTime @default(now()) @db.Timestamptz(6)
  updated_by    String?

  @@schema("tenant_data")
}

model pep_odontograma_history {
  changed_teeth Decimal  @db.Decimal
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  created_by    String
  description   String?
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  prontuario_id String   @db.Uuid
  snapshot_data Json

  @@schema("tenant_data")
}

model pep_tooth_surfaces {
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  odontograma_data_id String   @db.Uuid
  status              String
  surface             String
  updated_at          DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model pep_tratamentos {
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  created_by      String
  data_conclusao  String?
  data_inicio     String
  dente_codigo    String?
  descricao       String
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  observacoes     String?
  procedimento_id String?  @db.Uuid
  prontuario_id   String   @db.Uuid
  status          String
  titulo          String
  updated_at      DateTime @default(now()) @db.Timestamptz(6)
  valor_estimado  Decimal? @db.Decimal

  @@schema("tenant_data")
}

model permission_audit_logs {
  action            String
  clinic_id         String   @db.Uuid
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  details           Json?
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  module_catalog_id String?  @db.Uuid
  target_user_id    String   @db.Uuid
  template_name     String?
  user_id           String   @db.Uuid

  @@schema("tenant_data")
}

model permission_templates {
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  description String?
  icon        String?
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  module_keys String[]
  name        String

  @@schema("tenant_data")
}

model prescricoes_remotas {
  assinatura_digital     String?
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  descricao              String
  enviado_em             String?
  enviado_para_paciente  Boolean?
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  instrucoes             String?
  medicamento_dosagem    String?
  medicamento_duracao    String?
  medicamento_frequencia String?
  medicamento_nome       String?
  teleconsulta_id        String   @db.Uuid
  tipo                   String

  @@schema("tenant_data")
}

model problemas_radiograficos {
  analise_id          String   @db.Uuid
  confianca           Decimal? @db.Decimal
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  dente_codigo        String?
  descricao           String?
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  localizacao         String?
  severidade          String
  sugestao_tratamento String?
  tipo_problema       String
  urgente             Boolean?

  @@schema("tenant_data")
}

model procedimento_templates {
  categoria              String
  clinic_id              String?  @db.Uuid
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  created_by             String
  descricao              String?
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_public              Boolean?
  nome                   String
  steps                  Json
  tags                   String[]
  tempo_estimado_minutos Decimal  @db.Decimal
  updated_at             DateTime @default(now()) @db.Timestamptz(6)
  valor_sugerido         Decimal  @db.Decimal

  @@schema("tenant_data")
}

model produtos {
  ativo             Boolean
  categoria         String
  clinic_id         String   @db.Uuid
  codigo_barras     String?
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  descricao         String?
  fornecedor        String?
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  localizacao       String?
  nome              String
  observacoes       String?
  quantidade_atual  Decimal  @db.Decimal
  quantidade_minima Decimal  @db.Decimal
  unidade_medida    String
  updated_at        DateTime @default(now()) @db.Timestamptz(6)
  valor_unitario    Decimal  @db.Decimal

  @@schema("tenant_data")
}

model profiles {
  app_role   String
  avatar_url String?
  clinic_id  String?  @db.Uuid
  created_at DateTime @default(now()) @db.Timestamptz(6)
  full_name  String?
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active  Boolean
  phone      String?
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model prontuarios {
  clinic_id    String   @db.Uuid
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  created_by   String
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  patient_id   String   @db.Uuid
  patient_name String
  updated_at   DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model radiografia_ai_feedback {
  analise_id          String   @db.Uuid
  anotacoes_dentista  Json?
  clinic_id           String   @db.Uuid
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  created_by          String
  diagnostico_correto String?
  falsos_negativos    Json?
  falsos_positivos    Json?
  ia_estava_correta   Boolean
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  imagem_marcada_url  String?
  usado_para_treino   Boolean?

  @@schema("tenant_data")
}

model radiografia_laudo_templates {
  clinic_id             String   @db.Uuid
  created_at            DateTime @default(now()) @db.Timestamptz(6)
  created_by            String
  id                    String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_default            Boolean?
  nome_template         String
  template_markdown     String
  tipo_radiografia      String
  variaveis_disponiveis Json?

  @@schema("tenant_data")
}

model rate_limit_config {
  created_at            DateTime @default(now()) @db.Timestamptz(6)
  enabled               Boolean
  endpoint              String
  id                    String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  max_requests_per_ip   Decimal  @db.Decimal
  max_requests_per_user Decimal  @db.Decimal
  updated_at            DateTime @default(now()) @db.Timestamptz(6)
  window_minutes        Decimal  @db.Decimal

  @@schema("tenant_data")
}

model rate_limit_log {
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  endpoint      String
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address    String
  request_count Decimal? @db.Decimal
  updated_at    DateTime @default(now()) @db.Timestamptz(6)
  user_id       String?  @db.Uuid
  window_start  String

  @@schema("tenant_data")
}

model recalls {
  clinic_id              String   @db.Uuid
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  created_by             String?
  data_prevista          String
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mensagem_personalizada String?
  metodo_notificacao     String?
  notificacao_enviada    Boolean?
  patient_id             String   @db.Uuid
  status                 String
  tipo_recall            String
  updated_at             DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model room_availability {
  capacity    Decimal  @db.Decimal
  clinic_id   String   @db.Uuid
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  created_by  String
  equipment   Json?
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active   Boolean
  room_name   String
  room_number String?
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model root_actions_log {
  action           String
  details          Json?
  executed_at      String
  id               String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address       String
  root_user_id     String  @db.Uuid
  target_record_id String? @db.Uuid
  target_table     String?
  user_agent       String?

  @@schema("tenant_data")
}

model rum_metrics {
  clinic_id    String   @db.Uuid
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metric_name  String
  metric_value Decimal  @db.Decimal
  page_url     String
  rating       String
  user_agent   String?
  user_id      String?  @db.Uuid

  @@schema("tenant_data")
}

model sat_mfe_config {
  ativo            Boolean
  clinic_id        String   @db.Uuid
  codigo_ativacao  String?
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  fabricante       String?
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ip_address       String?
  metadata         Json?
  modelo           String?
  numero_serie     String
  porta            Decimal? @db.Decimal
  tipo_equipamento String
  updated_at       DateTime @default(now()) @db.Timestamptz(6)
  versao_software  String?

  @@schema("tenant_data")
}

model sat_mfe_impressoes {
  chave_consulta     String?
  clinic_id          String   @db.Uuid
  codigo_autorizacao String?
  config_id          String   @db.Uuid
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mensagem_retorno   String?
  metadata           Json?
  nfce_id            String?  @db.Uuid
  numero_sessao      String?
  status             String
  tentativas         Decimal  @db.Decimal
  tipo_equipamento   String
  updated_at         DateTime @default(now()) @db.Timestamptz(6)
  venda_id           String?  @db.Uuid
  xml_enviado        String?
  xml_retorno        String?

  @@schema("tenant_data")
}

model scheduled_backups {
  backup_type            String
  clinic_id              String   @db.Uuid
  compression_enabled    Boolean
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  created_by             String
  day_of_month           Decimal? @db.Decimal
  day_of_week            Decimal? @db.Decimal
  encryption_enabled     Boolean
  frequency              String
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  include_data           Json
  includes_postgres_dump Boolean?
  is_active              Boolean
  last_run_at            String?
  local_path             String?
  max_parallel_jobs      Decimal? @db.Decimal
  name                   String
  next_run_at            String
  notification_emails    String[]
  retention_policy_id    String?  @db.Uuid
  storage_config         Json?
  storage_destination    String?
  time_of_day            String
  updated_at             DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model scheduled_exports {
  clinic_id      String   @db.Uuid
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  dashboard_name String
  email          String
  export_format  String
  frequency      String
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active      Boolean
  last_sent_at   String?
  next_send_at   String?
  updated_at     DateTime @default(now()) @db.Timestamptz(6)
  user_id        String   @db.Uuid

  @@schema("tenant_data")
}

model security_audit_log {
  description       String
  id                String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  issue_type        String
  migration_version String
  resolution        String
  resolved_at       String
  severity          String?

  @@schema("tenant_data")
}

model split_payment_config {
  clinic_id       String   @db.Uuid
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_active       Boolean
  percentage      Decimal  @db.Decimal
  procedure_type  String?
  professional_id String   @db.Uuid
  updated_at      DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model split_payment_details {
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  id                   String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  net_amount           Decimal  @db.Decimal
  paid_at              String?
  recipient_id         String   @db.Uuid
  recipient_name       String
  recipient_type       String
  split_amount         Decimal  @db.Decimal
  split_transaction_id String   @db.Uuid
  status               String
  tax_withheld         Decimal? @db.Decimal

  @@schema("tenant_data")
}

model split_payment_recipients {
  clinic_id          String   @db.Uuid
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  recipient_id       String?  @db.Uuid
  recipient_name     String
  recipient_type     String
  rule_id            String   @db.Uuid
  split_fixed_amount Decimal? @db.Decimal
  split_percentage   Decimal? @db.Decimal
  tax_rate           Decimal? @db.Decimal
  tax_regime         String?

  @@schema("tenant_data")
}

model split_payment_rules {
  clinic_id  String   @db.Uuid
  conditions Json
  created_at DateTime @default(now()) @db.Timestamptz(6)
  created_by String
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  priority   Decimal  @db.Decimal
  rule_name  String
  rule_type  String
  status     String
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model split_payment_transactions {
  clinic_id           String   @db.Uuid
  conta_receber_id    String?  @db.Uuid
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  error_message       String?
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  processed_at        String?
  rule_id             String?  @db.Uuid
  split_calculated_at String
  status              String
  total_amount        Decimal  @db.Decimal
  transaction_id      String?  @db.Uuid

  @@schema("tenant_data")
}

model split_transactions {
  clinic_amount       Decimal  @db.Decimal
  clinic_id           String   @db.Uuid
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  percentage          Decimal  @db.Decimal
  processed_at        String?
  professional_amount Decimal  @db.Decimal
  professional_id     String   @db.Uuid
  status              String
  total_amount        Decimal  @db.Decimal
  transaction_id      String   @db.Uuid

  @@schema("tenant_data")
}

model system_health_metrics {
  clinic_id   String  @db.Uuid
  id          String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  metadata    Json?
  metric_type String
  recorded_at String
  unit        String?
  value       Decimal @db.Decimal

  @@schema("tenant_data")
}

model teleconsultas {
  appointment_id  String?  @db.Uuid
  clinic_id       String   @db.Uuid
  conduta         String?
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  created_by      String
  data_agendada   String
  data_finalizada String?
  data_iniciada   String?
  dentist_id      String   @db.Uuid
  diagnostico     String?
  duracao_minutos Decimal? @db.Decimal
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  link_sala       String?
  motivo          String
  observacoes     String?
  patient_id      String   @db.Uuid
  recording_url   String?
  status          String
  tipo            String
  titulo          String
  updated_at      DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model teleodonto_chat {
  attachment_type String?
  attachment_url  String?
  id              String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  message_text    String
  message_type    String
  read_at         String?
  sender_id       String  @db.Uuid
  sender_role     String
  sent_at         String
  session_id      String  @db.Uuid

  @@schema("tenant_data")
}

model teleodonto_files {
  compartilhado_com_paciente Boolean?
  descricao                  String?
  file_name                  String
  file_size_bytes            Decimal  @db.Decimal
  file_type                  String
  file_url                   String?
  id                         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  session_id                 String   @db.Uuid
  storage_path               String
  tipo_arquivo               String
  uploaded_at                String
  uploaded_by                String

  @@schema("tenant_data")
}

model teleodonto_sessions {
  appointment_id            String?  @db.Uuid
  clinic_id                 String   @db.Uuid
  consentimento_assinado_em String?
  consentimento_gravacao    Boolean?
  created_at                DateTime @default(now()) @db.Timestamptz(6)
  created_by                String
  dentist_id                String   @db.Uuid
  dentist_joined_at         String?
  diagnostico_preliminar    String?
  duracao_minutos           Decimal? @db.Decimal
  ended_at                  String?
  id                        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  notas_pos_consulta        String?
  notas_pre_consulta        String?
  patient_id                String   @db.Uuid
  patient_joined_at         String?
  platform                  String
  prescricoes               Json?
  problemas_tecnicos        String?
  qualidade_audio           String?
  qualidade_video           String?
  recording_url             String?
  room_id                   String?  @db.Uuid
  room_url                  String?
  scheduled_end             String
  scheduled_start           String
  started_at                String?
  status                    String
  updated_at                DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model terminal_command_history {
  clinic_id      String   @db.Uuid
  command        String
  duration_ms    Decimal? @db.Decimal
  executed_at    String
  exit_code      Decimal? @db.Decimal
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  output         String?
  user_id        String   @db.Uuid
  was_successful Boolean?

  @@schema("tenant_data")
}

model tiss_batches {
  batch_number      String
  clinic_id         String   @db.Uuid
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  guide_ids         String[]
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  insurance_company String
  processed_at      String?
  sent_at           String?
  status            String
  total_amount      Decimal  @db.Decimal
  total_guides      Decimal  @db.Decimal

  @@schema("tenant_data")
}

model tiss_guides {
  amount            Decimal  @db.Decimal
  batch_id          String?  @db.Uuid
  clinic_id         String   @db.Uuid
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  guide_number      String
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  insurance_company String
  patient_id        String   @db.Uuid
  procedure_code    String
  procedure_name    String
  response_date     String?
  service_date      String
  status            String
  submission_date   String?
  updated_at        DateTime @default(now()) @db.Timestamptz(6)

  @@schema("tenant_data")
}

model triagem_teleconsulta {
  alergias         String?
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  fotos_anexas     Json?
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  intensidade_dor  Decimal? @db.Decimal
  medicamentos_uso String?
  sintomas         String[]
  teleconsulta_id  String   @db.Uuid
  tempo_sintoma    String?

  @@schema("tenant_data")
}

model user_clinic_access {
  clinic_id  String   @db.Uuid
  created_at DateTime @default(now()) @db.Timestamptz(6)
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_default Boolean?
  user_id    String   @db.Uuid

  @@schema("tenant_data")
}

model user_module_permissions {
  can_delete        Boolean
  can_edit          Boolean
  can_view          Boolean
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  module_catalog_id String   @db.Uuid
  updated_at        DateTime @default(now()) @db.Timestamptz(6)
  user_id           String   @db.Uuid

  @@schema("tenant_data")
}

model user_roles {
  created_at DateTime @default(now()) @db.Timestamptz(6)
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  role       String
  user_id    String   @db.Uuid

  @@schema("tenant_data")
}

model wiki_page_versions {
  change_summary String?
  changed_by     String
  content        String
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  page_id        String   @db.Uuid
  title          String
  version        Decimal  @db.Decimal

  @@schema("tenant_data")
}

model wiki_pages {
  category     String
  clinic_id    String   @db.Uuid
  content      String
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  created_by   String
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  is_published Boolean
  parent_id    String?  @db.Uuid
  slug         String
  tags         String[]
  title        String
  updated_at   DateTime @default(now()) @db.Timestamptz(6)
  updated_by   String?
  version      Decimal  @db.Decimal

  @@schema("tenant_data")
}
